---
import NewsletterSidebar from '../../../components/NewsletterSidebar.astro';
import BaseLayout from '../../../layouts/BaseLayout.astro';

const slug = 'why-removing-anxiety-sells-in-japan-b2b';
const pubDate = '2026-08-21';
const pubDateDisplay = '2026年8月21日';

const title = '日本のB2Bで「不安を消す」ことが最大の販促になる理由';
const metaTitle = '日本のB2Bで「不安を消す」ことが最大の販促になる理由｜稟議と商流の構造 | Consilegy';
const metaDescription = '日本のB2B購買で決め手になるのは製品の良さではなく、稟議を通す人の不安が消えるかどうかです。稟議の構造とSIer・代理店を経由する商流から、何を用意すれば決裁が進むのかを解説します。';
const category = '業界構造';

const faqItems = [
	{
		q: '稟議とは何ですか？',
		a: '稟議とは、担当者が作成した提案書を関係部署と上位者が順に確認し、承認印や承認操作を重ねて組織の意思決定とする手続きです。会議で議論して決めるのではなく、書面が人から人へ回る過程で合意が積み上がります。決裁者が最後に見る時点では、すでに複数の部署が目を通した状態になっています。',
	},
	{
		q: 'なぜ日本のB2Bでは検討期間が長くなるのですか？',
		a: '購買の判断が個人ではなく組織に属しているためです。担当者は自分が納得するだけでは足りず、関係部署それぞれの懸念に答える材料を集める必要があります。1〜2年かかることも珍しくありませんが、これは意思決定が遅いのではなく、合意を形成する工程が購買プロセスに組み込まれているということです。',
	},
	{
		q: '「不安を消す」とは具体的に何を用意することですか？',
		a: '同規模・同業種の導入事例、想定していない事態が起きたときの対応、担当者が替わっても運用が続く設計の3つです。いずれも製品の性能ではなく、導入後に起きることの見通しを示す材料です。決裁者が知りたいのは製品が優れているかではなく、この判断で自分が責められないかどうかです。',
	},
	{
		q: '代理店やSIerを経由すると何が変わりますか？',
		a: '選定基準が導入実績に寄ります。提案する側にとって最も安全な材料が実績であり、買う側にとっても前例は稟議を通しやすい材料だからです。結果として、導入後の運用設計は提案にも稟議にも登場しにくくなります。ツールが入ったのに使われない状態は、この商流から生まれます。',
	},
];

const articleSchema = {
	'@context': 'https://schema.org',
	'@type': 'Article',
	headline: title,
	description: metaDescription,
	datePublished: pubDate,
	dateModified: pubDate,
	author: { '@type': 'Person', name: '山口聖子', alternateName: 'Seiko Yamaguchi', jobTitle: 'Revenue Architect', url: 'https://consilegy.com/company/', worksFor: { '@type': 'Organization', name: 'Consilegy', url: 'https://consilegy.com' } },
	publisher: { '@type': 'Organization', name: 'Consilegy', url: 'https://consilegy.com' },
	url: `https://consilegy.com/insights/${slug}/`,
	inLanguage: 'ja',
};

const faqSchema = {
	'@context': 'https://schema.org',
	'@type': 'FAQPage',
	mainEntity: faqItems.map(({ q, a }) => ({
		'@type': 'Question',
		name: q,
		acceptedAnswer: { '@type': 'Answer', text: a },
	})),
};

const breadcrumbSchema = {
	'@context': 'https://schema.org',
	'@type': 'BreadcrumbList',
	itemListElement: [
		{ '@type': 'ListItem', position: 1, name: 'ホーム', item: 'https://consilegy.com/' },
		{ '@type': 'ListItem', position: 2, name: 'コラム', item: 'https://consilegy.com/insights/' },
		{ '@type': 'ListItem', position: 3, name: title, item: `https://consilegy.com/insights/${slug}/` },
	],
};
---

<BaseLayout
	title={metaTitle}
	description={metaDescription}
	lang="ja"
>
	<Fragment slot="head">
		<script type="application/ld+json" set:html={JSON.stringify(articleSchema)} />
		<script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
		<script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />
	</Fragment>

<div class="article-wrap">

	<!-- ===== ARTICLE HEADER ===== -->
	<header class="article-head container">
		<nav class="breadcrumb" aria-label="パンくずリスト">
			<a href="/">ホーム</a>
			<span aria-hidden="true">›</span>
			<a href="/insights/">コラム</a>
			<span aria-hidden="true">›</span>
			<span aria-current="page">{title}</span>
		</nav>
		<div class="article-meta">
			<span class="article-cat">{category}</span>
		</div>
		<h1>日本のB2Bで「不安を消す」ことが<br class="br-pc" />最大の販促になる理由</h1>
		<p class="article-desc">「製品は良いと思います。ただ、上を通せる材料がなくて」。日本のB2Bで最後に聞くのは、たいていこの言葉です。決め手は製品の優劣ではなく、稟議を回す人の不安が消えるかどうかにあります。</p>
	</header>

	<!-- ===== ARTICLE BODY ===== -->
	<article class="article-body container" id="article">
		<div class="article-eyecatch">
			<img
				src={`/images/insights/${slug}.svg`}
				alt=""
				width="1200"
				height="630"
				class="article-eyecatch__img"
				loading="eager"
				aria-hidden="true"
			/>
		</div>

		<!-- ★ 定義ブロック（LLMO必須・削除禁止） -->
		<section class="article-section article-def" id="definition">
			<h2>稟議とは</h2>
			<p class="article-def__text"><strong>稟議とは</strong>、担当者が作成した提案書を関係部署と上位者が順に確認し、承認を重ねて組織の意思決定とする手続きです。会議の場で議論して決めるのではなく、書面が人から人へ回る過程で合意が積み上がっていきます。決裁者が最後に目を通す時点では、すでに複数の部署が内容を確認した状態になっています。日本のB2B購買では、この稟議を通せるかどうかが、製品の優劣と同じかそれ以上に結果を左右します。</p>
		</section>

		<hr class="article-hr" />

		<!-- はじめに -->
		<section class="article-section">
			<h2>商談は良かったのに、そこから3か月動かない</h2>
			<p>デモの反応は良く、担当者も前向きでした。質問も具体的で、社内で使う場面を想像しているのが分かります。ところが見積もりを出したあと、返信の間隔が少しずつ空いていきます。3か月後、ようやく届いた返信にはこう書かれています。「製品は良いと思います。ただ、上を通せる材料がなくて」。</p>
			<p>ここで「検討が進まなかった」と記録して終わると、次も同じことが起きます。担当者の熱意が足りなかったわけでも、製品が弱かったわけでもありません。担当者は自分の中では決めていて、そのうえで止まっています。</p>
			<div class="article-callout">
				<p><strong>日本のB2B購買で決裁者が判断しているのは「これは良い製品か」ではなく、「これを承認して、自分が責められないか」です。</strong>だから購買を動かす最大の変数は、製品の魅力ではなく、判断する人の不安がどれだけ消えているかになります。</p>
			</div>
			<p>検討期間の目安は1〜2年、長ければ3年以上に及びます。海外の調査では単一の決裁者を想定した購買モデルが前提になることが多いのですが、日本ではその前提が成り立ちません。担当者は自分が納得するだけでは足りず、関係部署それぞれの懸念に答える材料を集める工程に入ります。</p>
		</section>

		<hr class="article-hr" />

		<!-- 稟議の構造 -->
		<section class="article-section">
			<h2>稟議は「買う理由」ではなく「買って怒られない理由」を集める場</h2>
			<p>稟議書を書いたことがある人なら分かりますが、あの書式が求めているのは熱意ではありません。導入の背景・比較検討した他社・費用・想定効果、そしてリスクと対策です。並べてみると、後半に行くほど「起きうる悪いこと」の話になっています。</p>
			<p>これは組織として合理的です。稟議は決裁者ひとりの判断ミスを防ぐために、複数の目を通す仕組みだからです。ただし副作用があります。書面が回る先の一人ひとりは、その案件の成功で評価されるわけではありません。一方、承認した案件が失敗すれば、名前は残ります。</p>
			<p>つまり稟議のルートに立つ人の多くにとって、<strong>上振れは他人の手柄で、下振れは自分の責任</strong>です。この非対称があるかぎり、判断の軸は「どれだけ良いか」ではなく「どれだけ危なくないか」に寄ります。</p>
			<p>売り手側がここを読み違えると、提案は逆方向に進みます。機能の優位性を足し、他社との差分を強調し、導入効果の試算を大きくする。どれも「上振れ」の材料です。回ってくる書面を見る人が探しているのは、そこではありません。</p>
		</section>

		<hr class="article-hr" />

		<!-- 3つの不安 -->
		<section class="article-section">
			<h2>決裁のルートで生まれる3つの不安</h2>
			<p>稟議が止まるときに書面の裏側で起きているのは、だいたい次の3つです。どれも担当者からは言い出しにくく、そのまま「社内で調整中です」という言葉に置き換わります。</p>
			<div class="article-numbered">
				<div class="article-numbered__item">
					<p class="article-numbered__num">01</p>
					<h3>失敗したときに、誰が責任を取るのか分からない</h3>
					<p>導入したものが使われなかった場合、責任の所在があいまいなまま残ります。情報システム部門は「業務側が使わなかった」と考え、業務側は「使いにくいものを入れられた」と考えます。この予感がある時点で、承認は一段重くなります。売り手が用意すべきなのは、うまくいく前提の計画ではなく、うまくいかなかったときにどこで気づけるかの説明です。</p>
				</div>
				<div class="article-numbered__item">
					<p class="article-numbered__num">02</p>
					<h3>自社と同じ状況の会社が、先にやった形跡がない</h3>
					<p>前例は、日本の稟議において最も強い材料です。冒険しない姿勢を批判するのは簡単ですが、承認する側から見れば、前例は「同じ判断をした人が他にもいる」という事実にほかなりません。重要なのは有名企業の名前ではなく、規模・業種・体制が近いことです。従業員1万人の事例は、200人の会社の不安をほとんど消しません。</p>
				</div>
				<div class="article-numbered__item">
					<p class="article-numbered__num">03</p>
					<h3>担当者が替わったら、運用が止まりそうに見える</h3>
					<p>日本企業には異動があります。3年後にこの担当者がいない可能性を、決裁者は前提として織り込みます。特定の誰かの熱意で回っている絵に見えるほど、承認しづらくなります。ここで効くのは、設定した人以外にも運用できる状態であることを示す資料です。</p>
				</div>
			</div>
			<p>3つに共通しているのは、いずれも製品の性能では答えられない不安だという点です。カタログのどこにも書いていないので、売り手はここに気づかないまま、機能の説明を厚くしていきます。</p>
		</section>

		<hr class="article-hr" />

		<!-- 商流 -->
		<section class="article-section">
			<h2>商流が、その不安を増幅している</h2>
			<p>日本のB2B SaaSやシステム導入では、代理店やSIerを経由する商流が広く使われています。買う側にとっては窓口が一本化され、支払いも既存の取引口座で処理できます。合理的な仕組みです。</p>
			<p>ただし、選定の基準に影響が出ます。提案する側にとって、最も安全で説明しやすい材料は導入実績です。買う側にとっても、前例は稟議を通しやすい材料です。両者の利害が一致するため、<strong>提案書の中心は「どれだけ入っているか」になり、「入れたあとどう回すか」は後景に退きます</strong>。</p>
			<p>結果として、運用設計は提案にも稟議にも登場しにくくなります。誰かが手を抜いたわけではなく、その工程を担当する人が商流の中に存在していません。売り手は導入までを担当し、買い手は稟議を通すまでを担当し、そこから先は空白になります。</p>
			<p>ツールを入れたのに使われないという状態は、この空白から生まれます。日本企業のCRM導入率が37.2%にとどまり、導入済みの企業でも定着に課題を抱えているのは、製品の品質だけでは説明できません。</p>
			<p>私たちが販売代理店をやらないのは、この構造から距離を置きたいからです。導入本数で収益を得ていると、活かす判断と止める判断を同じ基準で出せなくなります。</p>
		</section>

		<hr class="article-hr" />

		<!-- 最初の一手 -->
		<section class="article-section">
			<h2>最初の一手：不安を消す3つの材料をそろえる</h2>
			<p>提案資料に機能を足す前に、次の3つがあるかを確認してください。順番にも意味があります。</p>
			<div class="article-numbered">
				<div class="article-numbered__item">
					<p class="article-numbered__num">01</p>
					<h3>同規模・同業種の事例を1つ、具体的に用意する</h3>
					<p>ロゴを10社並べるより、規模と業種が近い1社の話を詳しく書くほうが効きます。導入前に何に困っていて、どういう順番で進めて、どこでつまずいたか。つまずいた話まで入れて構いません。むしろ、そこがあるほうが信用されます。</p>
				</div>
				<div class="article-numbered__item">
					<p class="article-numbered__num">02</p>
					<h3>うまくいかなかったときの引き返し方を書く</h3>
					<p>3か月後にどの数字を見て、それが想定を下回っていたら何をするか。ここを先に決めておくと、決裁者は「失敗が発覚するのは1年後」という最悪の想像をしなくて済みます。引き返せる設計は、弱さではなく判断のしやすさです。</p>
				</div>
				<div class="article-numbered__item">
					<p class="article-numbered__num">03</p>
					<h3>担当者が替わっても回る形にして見せる</h3>
					<p>設定の意図と運用ルールを1枚にまとめ、提案の時点で渡します。個人の熱意ではなく仕組みで回る絵になっていれば、異動を前提とした組織でも承認しやすくなります。</p>
				</div>
			</div>
			<p>買う側の立場であれば、同じ3つを売り手に要求してください。出せない相手は、導入後の運用まで考えていない可能性があります。</p>
		</section>

		<hr class="article-hr" />

		<!-- まとめ -->
		<section class="article-section">
			<h2>まとめ：不安を消すことは、値引きより強い</h2>
			<p>日本のB2Bで商談が止まる原因は、担当者の熱意不足でも製品力の差でもありません。稟議のルートに立つ人が「承認して自分が責められないか」を判断しているのに、売り手が「どれだけ良いか」を説明し続けているという、問いと答えのすれ違いです。</p>
			<p>不安を消す材料は、値引きよりも効きます。価格を下げても責任の所在は変わらず、前例も増えないからです。同規模の事例・引き返し方・担当者交代への備え。この3つは、機能を1つ足すよりも決裁を前に進めます。</p>
			<p>最初の一歩は、直近で止まった商談を1件開いて、相手が最後に言った言葉を読み返すことです。「検討します」ではなく「上を通せる材料がなくて」と書かれていたなら、足りなかったのは製品ではありません。</p>
		</section>

		<hr class="article-hr" />

		<!-- ★ 記事末FAQ（LLMO必須・faqItems と自動同期） -->
		<section class="article-section article-faq" id="faq">
			<h2>よくある質問</h2>
			<div class="faq-list">
				{faqItems.map(({ q, a }) => (
					<details class="faq-item">
						<summary class="faq-q">{q}</summary>
						<p class="faq-a">{a}</p>
					</details>
				))}
			</div>
		</section>

		<p class="article-pubdate">公開日：{pubDateDisplay}</p>
	</article>

	<!-- ===== RELATED SERVICES ===== -->
	<nav class="article-services" aria-label="関連サービス">
		<h2 class="article-services__heading">関連サービス</h2>
		<div class="article-services__grid">
		<a href="/services/b2b-marketing-gtm-design/" class="article-services__card">
				<span class="article-services__cat">サービス</span>
				<span class="article-services__title">B2Bマーケティング戦略 / GTM設計</span>
				<span class="article-services__arrow">→</span>
			</a>
		<a href="/services/revenue-architecture-design/" class="article-services__card">
				<span class="article-services__cat">サービス</span>
				<span class="article-services__title">レベニューアーキテクチャ設計</span>
				<span class="article-services__arrow">→</span>
			</a>
		</div>
	</nav>
	<NewsletterSidebar />

	<!-- ===== RELATED LINKS ===== -->
	<aside class="article-related container">
		<p class="article-related__label">関連コンテンツ</p>
		<div class="article-related__grid">
			<a href="/revenue-architecture/" class="article-related__card">
				<p class="article-related__card-cat">概念解説</p>
				<p class="article-related__card-title">Revenue Architecture（レベニューアーキテクチャ）とは</p>
				<span class="article-related__card-arrow">→</span>
			</a>
			<a href="/about-revops/" class="article-related__card">
				<p class="article-related__card-cat">概念解説</p>
				<p class="article-related__card-title">RevOps（レベニューオペレーション）とは</p>
				<span class="article-related__card-arrow">→</span>
			</a>
			<a href="/insights/why-crm-adoption-fails/" class="article-related__card">
				<p class="article-related__card-cat">関連記事</p>
				<p class="article-related__card-title">CRMが定着しない本当の理由</p>
				<span class="article-related__card-arrow">→</span>
			</a>
		</div>
	</aside>

	<!-- ===== CTA ===== -->
	<section class="article-cta">
		<div class="container">
			<div class="article-cta__body">
				<h2>Consilegyでは、Revenue Architectureの観点から<br class="br-pc" />売上の取りこぼしを可視化する無料の収益診断を公開しています。</h2>
				<ul class="article-cta__list">
					<li>商談は良い反応なのに、稟議の段階で止まることが多い</li>
					<li>失注理由が「社内調整」で終わり、次に活かせていない</li>
					<li>導入実績は伝えているが、それ以外の材料を持っていない</li>
				</ul>
				<p class="article-cta__note">ツールの前に、「設計図」から整理することが、最短の改善ルートです。</p>
				<div class="article-cta__actions">
					<a href="/diagnostic/" class="btn btn--cta-primary">収益を自己診断</a>
					<a href="/services/b2b-marketing-gtm-design/" class="btn btn--cta-ghost">B2Bマーケティング戦略 / GTM設計を見る</a>
				</div>
			</div>
		</div>
	</section>

</div>
</BaseLayout>
<style>
	/* ── 定義ブロック（LLMO） ─── */
	.article-def__text {
		background: var(--color-bg-soft, #f8fafc);
		border-left: 4px solid var(--color-primary, #1e40af);
		border-radius: 0 8px 8px 0;
		padding: 1.25rem 1.5rem;
		font-size: 1rem;
		line-height: 1.85;
		color: var(--color-navy, #0f172a);
	}

	/* ── 記事末FAQ（LLMO） ─── */
	.faq-list {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.faq-item {
		border: 1px solid var(--color-border, #e2e8f0);
		border-radius: 8px;
		background: #fff;
		overflow: hidden;
	}

	.faq-q {
		font-size: 0.95rem;
		font-weight: 600;
		color: var(--color-navy, #0f172a);
		padding: 1.1rem 1.25rem;
		cursor: pointer;
		list-style: none;
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 1rem;
		line-height: 1.5;
	}

	.faq-q::-webkit-details-marker { display: none; }

	.faq-q::after {
		content: '+';
		font-size: 1.25rem;
		font-weight: 300;
		color: var(--color-primary, #1e40af);
		flex-shrink: 0;
		transition: transform 0.2s;
	}

	details[open] .faq-q::after { transform: rotate(45deg); }

	.faq-a {
		font-size: 0.9rem;
		line-height: 1.8;
		color: var(--color-charcoal, #334155);
		padding: 0 1.25rem 1.25rem;
		margin: 0;
	}

	/* ── Eyecatch ─── */
	.article-eyecatch {
		margin-bottom: 2rem;
		border-radius: 8px;
		overflow: hidden;
		max-height: 360px;
	}
	.article-eyecatch__img {
		width: 100%; height: auto; display: block;
		max-height: 360px; object-fit: cover;
	}
	/* ── Layout ────────────────────────────────────────────── */
	.article-wrap { background: #fff; }

	.container {
		max-width: 800px;
		margin-inline: auto;
		padding-inline: var(--space-md, 1.5rem);
	}

	.br-pc { display: inline; }

	/* ── Breadcrumb ────────────────────────────────────────── */
	.breadcrumb {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 0.4em;
		font-size: 0.8rem;
		color: var(--color-text-muted);
		margin-bottom: 2rem;
	}

	.breadcrumb a { color: inherit; text-decoration: none; }
	.breadcrumb a:hover { color: var(--color-primary, #1e40af); }

	/* ── Article header ────────────────────────────────────── */
	.article-head {
		padding-top: 3rem;
		padding-bottom: 2.5rem;
		border-bottom: 1px solid var(--color-border, #e2e8f0);
	}

	.article-meta {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		margin-bottom: 1.25rem;
		flex-wrap: wrap;
	}

	.article-cat {
		font-size: 0.72rem;
		font-weight: 700;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		color: var(--color-primary, #1e40af);
		background: rgba(30, 64, 175, 0.07);
		padding: 0.2em 0.7em;
		border-radius: 4px;
	}

	.article-date,
	.article-read {
		font-size: 0.8rem;
		color: var(--color-text-muted);
	}

	.article-date::before {
		content: '';
		display: inline-block;
		width: 1px;
		height: 0.9em;
		background: var(--color-border, #e2e8f0);
		margin-right: 0.75rem;
		vertical-align: middle;
	}

	.article-head h1 {
		font-size: clamp(1.5rem, 3vw, 2.1rem);
		font-weight: 600;
		color: var(--color-navy, #0f172a);
		line-height: 1.35;
		margin: 0 0 1rem;
	}

	.article-desc {
		font-size: 1rem;
		line-height: 1.75;
		color: var(--color-charcoal, #334155);
		margin: 0;
	}

	/* ── Article body ──────────────────────────────────────── */
	.article-body { padding-block: 3rem; }

	.article-section { margin-bottom: 0; }

	.article-section h2 {
		font-size: clamp(1.15rem, 2.2vw, 1.4rem);
		font-weight: 600;
		color: var(--color-navy, #0f172a);
		margin: 0 0 1.25rem;
		line-height: 1.45;
		padding-bottom: 0.6rem;
		border-bottom: 2px solid var(--color-border, #e2e8f0);
	}

	.article-section h3 {
		font-size: 1rem;
		font-weight: 600;
		color: var(--color-navy, #0f172a);
		margin: 2rem 0 0.75rem;
	}

	.article-section p {
		font-size: 0.975rem;
		line-height: 1.9;
		color: var(--color-charcoal, #334155);
		margin: 0 0 1rem;
	}

	.article-section ul {
		margin: 0 0 1.25rem;
		padding: 0;
		list-style: none;
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.article-section ul li {
		padding-left: 1.25rem;
		position: relative;
		font-size: 0.95rem;
		line-height: 1.7;
		color: var(--color-charcoal, #334155);
	}

	.article-section ul li::before {
		content: '';
		position: absolute;
		left: 0;
		top: 0.65em;
		width: 5px;
		height: 5px;
		border-radius: 50%;
		background: var(--color-primary, #1e40af);
		opacity: 0.5;
	}

	.article-hr {
		border: none;
		border-top: 1px solid var(--color-border, #e2e8f0);
		margin: 3rem 0;
	}

	/* ── Callout ───────────────────────────────────────────── */
	.article-callout {
		background: var(--color-bg-soft, #f8fafc);
		border-left: 4px solid var(--color-primary, #1e40af);
		border-radius: 0 8px 8px 0;
		padding: 1.1rem 1.5rem;
		margin: 1.5rem 0;
	}

	.article-callout p {
		margin: 0;
		font-size: 0.95rem;
		line-height: 1.8;
		color: var(--color-navy, #0f172a);
	}

	/* ── Numbered blocks ───────────────────────────────────── */
	.article-numbered {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
		margin-top: 1.5rem;
	}

	.article-numbered__item {
		border: 1px solid var(--color-border, #e2e8f0);
		border-radius: 8px;
		padding: 1.5rem;
		background: #fff;
	}

	.article-numbered__num {
		font-size: 0.7rem;
		font-weight: 800;
		letter-spacing: 0.12em;
		color: var(--color-primary, #1e40af);
		margin: 0 0 0.5rem;
	}

	.article-numbered__item h3 {
		margin: 0 0 0.75rem;
		font-size: 1rem;
		font-weight: 600;
		color: var(--color-navy, #0f172a);
	}

	.article-numbered__item p {
		margin: 0;
		font-size: 0.9rem;
		line-height: 1.8;
		color: var(--color-charcoal, #334155);
	}

	/* ── Table ─────────────────────────────────────────────── */
	.article-table-wrap {
		overflow-x: auto;
		margin: 1.5rem 0;
		border: 1px solid var(--color-border, #e2e8f0);
		border-radius: 8px;
	}

	.article-table {
		width: 100%;
		border-collapse: collapse;
		font-size: 0.9rem;
	}

	.article-table thead th {
		padding: 0.75rem 1.25rem;
		background: var(--color-navy, #0f172a);
		color: #fff;
		font-weight: 700;
		text-align: left;
		white-space: nowrap;
	}

	.article-table tbody td {
		padding: 0.75rem 1.25rem;
		border-bottom: 1px solid var(--color-border, #e2e8f0);
		color: var(--color-charcoal, #334155);
		line-height: 1.6;
	}

	.article-table tbody tr:last-child td { border-bottom: none; }

	.article-table tbody td:first-child {
		font-weight: 700;
		color: var(--color-navy, #0f172a);
		white-space: nowrap;
		border-right: 1px solid var(--color-border, #e2e8f0);
	}

	/* ── Related ───────────────────────────────────────────── */
	.article-related {
		padding-block: 2.5rem 3rem;
		border-top: 1px solid var(--color-border, #e2e8f0);
	}

	.article-related__label {
		font-size: 0.72rem;
		font-weight: 700;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		color: var(--color-text-muted);
		margin: 0 0 1rem;
	}

	.article-related__grid {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
		gap: 0.75rem;
	}

	.article-related__card {
		display: flex;
		flex-direction: column;
		gap: 0.3rem;
		padding: 1rem 1.25rem;
		border: 1px solid var(--color-border, #e2e8f0);
		border-radius: 8px;
		text-decoration: none;
		transition: border-color 0.15s, box-shadow 0.15s;
	}

	.article-related__card:hover {
		border-color: var(--color-primary, #1e40af);
		box-shadow: 0 2px 10px rgba(30, 64, 175, 0.07);
	}

	.article-related__card-cat {
		font-size: 0.68rem;
		font-weight: 700;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		color: var(--color-primary, #1e40af);
		margin: 0;
	}

	.article-related__card-title {
		font-size: 0.875rem;
		font-weight: 600;
		color: var(--color-navy, #0f172a);
		margin: 0;
		line-height: 1.45;
	}

	.article-related__card-arrow {
		font-size: 0.85rem;
		color: var(--color-primary, #1e40af);
		margin-top: 0.25rem;
	}

	/* ── CTA ───────────────────────────────────────────────── */
	.article-cta {
		padding-block: 4rem;
		background:
			radial-gradient(800px 360px at 85% -5%, rgba(124,92,191,0.35), transparent 55%),
			linear-gradient(135deg, #1e40af 0%, #1d4ed8 100%);
	}

	.article-cta__body {
		max-width: 700px;
		margin-inline: auto;
		text-align: center;
	}

	.article-cta__body h2 {
		font-size: clamp(1.2rem, 2.5vw, 1.65rem);
		font-weight: 600;
		color: #fff;
		line-height: 1.5;
		margin: 0 0 1.5rem;
	}

	.article-cta__list {
		display: flex;
		flex-direction: column;
		gap: 0.4rem;
		margin: 0 auto 1.5rem;
		padding: 0;
		list-style: none;
		text-align: left;
		max-width: 420px;
	}

	.article-cta__list li {
		font-size: 0.875rem;
		color: rgba(255,255,255,0.85);
		padding-left: 1.1rem;
		position: relative;
		line-height: 1.6;
	}

	.article-cta__list li::before {
		content: '·';
		position: absolute;
		left: 0;
		color: rgba(255,255,255,0.5);
		font-size: 1.2em;
	}

	.article-cta__note {
		font-size: 0.9rem;
		color: rgba(255,255,255,0.7);
		margin: 0 0 2rem;
	}

	.article-cta__actions {
		display: flex;
		flex-wrap: wrap;
		gap: 1rem;
		justify-content: center;
	}

	.btn--cta-primary {
		display: inline-flex;
		align-items: center;
		background: #fff;
		color: var(--color-primary, #1e40af);
		border: 2px solid transparent;
		padding: 0.85em 1.75em;
		font-size: 0.95rem;
		border-radius: 6px;
		font-weight: 700;
		text-decoration: none;
		transition: background 0.15s;
	}

	.btn--cta-primary:hover { background: rgba(255,255,255,0.9); }

	.btn--cta-ghost {
		display: inline-flex;
		align-items: center;
		background: transparent;
		color: rgba(255,255,255,0.9);
		border: 2px solid rgba(255,255,255,0.4);
		padding: 0.85em 1.75em;
		font-size: 0.95rem;
		border-radius: 6px;
		font-weight: 600;
		text-decoration: none;
		transition: border-color 0.15s;
	}

	.btn--cta-ghost:hover { border-color: rgba(255,255,255,0.8); }

	/* ══════════════════════════════════════════════════════════
	   DIAGRAMS
	══════════════════════════════════════════════════════════ */
	.diag {
		margin: 2rem 0;
		border: 1px solid var(--color-border, #e2e8f0);
		border-radius: 8px;
		overflow: hidden;
		background: #fff;
	}

	.diag-caption {
		display: block;
		font-size: 0.78rem;
		color: var(--color-text-muted);
		text-align: center;
		padding: 0.75rem 1rem;
		background: var(--color-bg-soft, #f8fafc);
		border-top: 1px solid var(--color-border, #e2e8f0);
		line-height: 1.5;
		margin: 0;
	}

	/* ── Diagram 1: Flow (日報化) ─────────────────────────── */
	.diag--flow { padding: 1.5rem 1.5rem 0; }

	.diag-flow-track {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		margin-bottom: 1rem;
		flex-wrap: nowrap;
		overflow-x: auto;
		padding-bottom: 0.25rem;
	}

	.diag-flow-node {
		flex: 1;
		min-width: 100px;
		border: 1px solid var(--color-border, #e2e8f0);
		border-radius: 8px;
		padding: 1rem 0.75rem;
		text-align: center;
		background: var(--color-bg-soft, #f8fafc);
		flex-shrink: 0;
	}

	.diag-flow-node--center {
		border-color: var(--color-primary, #1e40af);
		background: rgba(30, 64, 175, 0.04);
	}

	.diag-flow-node__icon {
		font-size: 1.25rem;
		margin-bottom: 0.4rem;
		line-height: 1;
	}

	.diag-flow-node__label {
		font-size: 0.82rem;
		font-weight: 700;
		color: var(--color-navy, #0f172a);
		margin-bottom: 0.25rem;
	}

	.diag-flow-node__sub {
		font-size: 0.7rem;
		color: var(--color-text-muted);
		line-height: 1.4;
	}

	.diag-flow-arrow {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.2rem;
		color: var(--color-text-muted);
		flex-shrink: 0;
	}

	.diag-flow-arrow__label {
		font-size: 0.65rem;
		color: var(--color-text-muted);
	}

	.diag-flow-nofeedback {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 0.5rem;
		padding: 0.65rem 1rem;
		margin-bottom: 1.25rem;
		background: #fef2f2;
		border: 1px solid #fecaca;
		border-radius: 6px;
		font-size: 0.82rem;
		font-weight: 600;
		color: #dc2626;
	}

	.diag-flow-nofeedback__icon {
		font-size: 1rem;
		opacity: 0.7;
	}

	/* ── Diagram 2: Hex Grid (6 elements) ────────────────── */
	.diag--hex { padding: 1.5rem 1.5rem 0; }

	.diag-hex-center {
		text-align: center;
		margin-bottom: 1rem;
	}

	.diag-hex-center__label {
		font-size: 1.1rem;
		font-weight: 800;
		color: var(--color-navy, #0f172a);
		line-height: 1.2;
	}

	.diag-hex-center__sub {
		font-size: 0.78rem;
		color: var(--color-primary, #1e40af);
		font-weight: 700;
		letter-spacing: 0.06em;
	}

	.diag-hex-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 0.75rem;
		margin-bottom: 1.25rem;
	}

	.diag-hex-card {
		border: 1px solid var(--color-border, #e2e8f0);
		border-radius: 8px;
		padding: 1rem;
		background: #fff;
		position: relative;
	}

	.diag-hex-card__num {
		font-size: 0.65rem;
		font-weight: 800;
		letter-spacing: 0.1em;
		color: var(--color-primary, #1e40af);
		margin: 0 0 0.35rem;
	}

	.diag-hex-card__title {
		font-size: 0.82rem;
		font-weight: 600;
		color: var(--color-navy, #0f172a);
		margin: 0 0 0.4rem;
		line-height: 1.35;
	}

	.diag-hex-card__body {
		font-size: 0.75rem;
		line-height: 1.55;
		color: var(--color-charcoal, #334155);
		margin: 0;
	}

	/* ── Diagram 3: Pyramid (RA → RevOps → CRM) ──────────── */
	.diag--pyramid { padding: 1.5rem 1.5rem 0; }

	.diag-pyramid-tier {
		border-radius: 8px;
		padding: 1rem 1.25rem;
		margin-bottom: 0;
	}

	.diag-pyramid-tier--ra {
		background: var(--color-navy, #0f172a);
		color: #fff;
	}

	.diag-pyramid-tier--mid {
		background: var(--color-bg-soft, #f8fafc);
		border: 1px solid var(--color-border, #e2e8f0);
	}

	.diag-pyramid-tier--revops {
		background: #fff;
		border: 1px solid var(--color-border, #e2e8f0);
	}

	.diag-pyramid-tier--outcome {
		background: rgba(30, 64, 175, 0.04);
		border: 1px solid rgba(30, 64, 175, 0.2);
		margin-bottom: 1.25rem;
	}

	.diag-pyramid-tier__tag {
		font-size: 0.68rem;
		font-weight: 700;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		margin-bottom: 0.3rem;
	}

	.diag-pyramid-tier--ra .diag-pyramid-tier__tag    { color: rgba(255,255,255,0.45); }
	.diag-pyramid-tier--mid .diag-pyramid-tier__tag    { color: var(--color-text-muted); }
	.diag-pyramid-tier--revops .diag-pyramid-tier__tag { color: var(--color-text-muted); }
	.diag-pyramid-tier--outcome .diag-pyramid-tier__tag { color: var(--color-primary, #1e40af); }

	.diag-pyramid-tier__role {
		font-size: 0.9rem;
		font-weight: 700;
		margin-bottom: 0.65rem;
	}

	.diag-pyramid-tier--ra .diag-pyramid-tier__role    { color: #fff; }
	.diag-pyramid-tier--revops .diag-pyramid-tier__role { color: var(--color-navy, #0f172a); }

	.diag-pyramid-tier__items {
		display: flex;
		flex-wrap: wrap;
		gap: 0.35rem;
	}

	.diag-pyramid-tier__items span {
		font-size: 0.75rem;
		font-weight: 500;
		padding: 0.2em 0.7em;
		border-radius: 4px;
	}

	.diag-pyramid-tier--ra .diag-pyramid-tier__items span {
		background: rgba(255,255,255,0.1);
		color: rgba(255,255,255,0.8);
		border: 1px solid rgba(255,255,255,0.12);
	}

	.diag-pyramid-tier--mid .diag-pyramid-tier__items span {
		background: #fff;
		color: var(--color-charcoal, #334155);
		border: 1px solid var(--color-border, #e2e8f0);
	}

	.diag-pyramid-tier--revops .diag-pyramid-tier__items span {
		background: var(--color-bg-soft, #f8fafc);
		color: var(--color-charcoal, #334155);
		border: 1px solid var(--color-border, #e2e8f0);
	}

	.diag-pyramid-tier--outcome .diag-pyramid-tier__items span {
		background: rgba(30, 64, 175, 0.07);
		color: var(--color-primary, #1e40af);
		border: 1px solid rgba(30, 64, 175, 0.15);
		font-weight: 600;
	}

	.diag-pyramid-arrow {
		text-align: center;
		font-size: 1.1rem;
		color: var(--color-primary, #1e40af);
		padding: 0.35rem 0;
	}

	/* ── Responsive ────────────────────────────────────────── */
	@media (max-width: 700px) {
		.br-pc { display: none; }
		.article-related__grid { grid-template-columns: 1fr; }
		.article-head { padding-top: 2rem; }
		.diag-hex-grid { grid-template-columns: repeat(2, 1fr); }
		.diag-flow-track { gap: 0.35rem; }
		.diag-flow-node { min-width: 88px; padding: 0.75rem 0.5rem; }
	}

	@media (max-width: 480px) {
		.diag-hex-grid { grid-template-columns: 1fr 1fr; }
	}
</style>
