---
import BaseLayout from '../layouts/BaseLayout.astro';

const insightsPosts = [
	{
		slug: 'why-good-marketing-does-not-create-repeatable-growth',
		title: 'なぜ「良い施策」を打っているのに、組織が再現性を持てないのか',
		description: '優秀な施策や営業人材がいるのに成長が再現しない原因を、Revenue ArchitectureとRevOpsの観点から解説します。',
		category: 'レベニューアーキテクチャ',
	},
	{
		slug: 'why-dashboards-do-not-improve-executive-decisions',
		title: 'ダッシュボードを作っても意思決定が速くならない理由',
		description: 'BIやダッシュボードを整備しても経営判断が速くならない原因を、KPI設計・Revenue Architecture・RevOpsの観点から解説します。',
		category: 'KPI・BI',
	},
	{
		slug: 'why-marketing-automation-does-not-improve-pipeline',
		title: 'MAを導入しても商談化率が改善しない理由',
		description: 'HubSpotやMarketoなどのMAを導入しても商談化率が改善しない原因を、Revenue ArchitectureとRevOpsの観点から解説します。',
		category: 'CRM',
	},
];
---

<BaseLayout
	title="使われていない顧客データを、売上が動く仕組みへ | Consilegy"
	description="「顧客情報がExcelとメールにしかない」「入れたはずのMA/SFAに入力されない」——原因は営業力ではなく、収益構造の設計にあります。CRM・SFA・MA・BIを、マーケ・営業・CSが同じ顧客を見られる仕組みに組み直し、現場で回るところまで伴走します。支援実績40社。"
	lang="ja"
>
	<script slot="head">
		if (window.location.pathname === '/' || window.location.pathname === '') {
			const pref = localStorage.getItem('languagePreference');
			if (!pref) {
				const lang = navigator.language || 'ja';
				if (!lang.toLowerCase().startsWith('ja')) {
					window.location.replace('/en/');
				}
			}
		}
	</script>

<!-- ===================== HERO ===================== -->
<section class="hero">
	<div class="container hero-inner">
		<div class="hero-copy">
			<p class="hero-kicker">Revenue Architecture / RevOps</p>
			<h1>
				使われていない顧客データを、<br />
				<span class="hero-accent">売上が動く仕組みへ。</span>
			</h1>
			<ul class="hero-symptoms">
				<li>「顧客情報が、まだExcelとメールにしかない」</li>
				<li>「入れたはずのMA/SFAに、入力されない」</li>
				<li>「施策の成果が、売上として見えない」</li>
			</ul>
			<p class="hero-lead">
				Revenue CRMの開発と、CRM・SFA・MA・BIの再設計。
				ツール導入ではなく、売上が動く業務設計と運用定着まで伴走します。
			</p>
			<div class="hero-actions">
				<a href="/services/" class="btn btn--hero-primary btn--lg">
					サービス・料金を見る
				</a>
				<a href="/crm/" class="btn btn--hero-outline btn--lg">
					Revenue CRMを見る
				</a>
			</div>
			<ul class="hero-proof" aria-label="Consilegyの実績">
				<li>支援実績 40社</li>
				<li>CRM/SFA/MA再設計</li>
				<li>設計から運用定着まで</li>
			</ul>
		</div>

		<figure class="hero-media">
			<img
				src="/images/crm/screen-dashboard.webp"
				alt="Revenue CRMのダッシュボード。パイプライン総額、加重フォーキャスト、勝率、期限切れタスク、ステージ別パイプライン、注目の商談を一画面に表示。"
				width="1600"
				height="1000"
				decoding="async"
				fetchpriority="high"
			/>
			<figcaption>Revenue CRM ダッシュボード：最初に見る数字と、次に動く相手を同じ画面で</figcaption>
		</figure>
	</div>
	<div class="hero-tools">
		<div class="container">
			<p class="tools-label">対応ツール（一部）</p>
			<div class="tools-row">
				<span>Salesforce</span><span class="tools-sep">|</span>
				<span>HubSpot</span><span class="tools-sep">|</span>
				<span>Kintone</span><span class="tools-sep">|</span>
				<span>Marketo</span><span class="tools-sep">|</span>
				<span>ServiceNow</span><span class="tools-sep">|</span>
				<span>Zoho CRM</span><span class="tools-sep">|</span>
				<span>Tableau / Looker</span>
			</div>
		</div>
	</div>
</section>

<!-- ===================== FORK（はしご） ===================== -->
<section class="fork">
	<div class="container">
		<header class="fork-head">
			<p class="section-eyebrow">事業の状態別</p>
			<h2>あなたの会社は、どちらですか。</h2>
		</header>

		<div class="fork-grid">

			<article class="fork-card">
				<span class="fork-tag">これからCRMを入れる会社へ</span>
				<p class="fork-state">
					顧客情報はExcelとメール、案件の状況は担当者の頭の中。
					まずは、最初のCRMを入れるところから始めます。
				</p>
				<p class="fork-body">
					「今さらCRMも入れていないのか」と思う必要はありません。
					日本企業のCRM導入率は<strong>37.2%</strong>。6割以上が、まだ持っていません。
					<span class="fork-src">HubSpot Japan「日本の営業に関する意識・実態調査2025」2024年11月調査</span>
				</p>
				<p class="fork-body">
					最初のCRMとして使えるプロダクトも、現在クローズドβで準備しています。
					一般公開前のため、情報はβ版ページでご確認ください。
				</p>
				<p class="fork-body">
					教室・スクール向けには、生徒・レッスン・請求を中心にしたスクール版も用意しています。
				</p>
				<div class="fork-links">
					<a href="/crm/" class="fork-link">β版情報を見る</a>
					<a href="/crm/school/" class="fork-link">スクール版を見る</a>
				</div>
			</article>

			<article class="fork-card fork-card--consulting">
				<span class="fork-tag">入れたCRMが動いていない会社へ</span>
				<p class="fork-state">
					Salesforce、HubSpot、kintone、Marketo。入っているのに、営業は入力しない。
					ダッシュボードの数字は信用されず、結局スプレッドシートで会議をしている。
				</p>
				<p class="fork-body">
					これはツールの問題ではなく、設計の問題です。
				</p>
				<p class="fork-body">
					Consilegyは、既存のツール構成を前提に収益プロセスを設計し直し、
					現場が使い続けるところまで伴走します。乗り換えありきではありません。
				</p>
				<a href="/services/" class="fork-link">支援サービスと料金を見る</a>
			</article>

		</div>

		<div class="fork-third">
			<p>
				どちらか分からない場合は、3分の収益診断からどうぞ。
				どこで止まっているかを整理してお返しします。
			</p>
			<a href="/diagnostic/" class="btn fork-third__btn">3分の収益診断を受ける（無料）</a>
		</div>
	</div>
</section>

<!-- ===================== WHY ===================== -->
<section class="why">
	<div class="container">
		<div class="why-grid">
			<div class="why-head">
				<p class="section-eyebrow">Why It Fails</p>
				<h2>CRMが機能しない理由は、<br />ツールではなく<span class="text-primary">設計にあります</span></h2>
				<p class="why-claim">
					営業は、意味のないシステムには入力しません。<br />
					これは意識の問題ではなく、合理的な判断です。
				</p>
				<blockquote class="why-anecdote">
					<p>ある会社のCRMには、商談ステージが<strong>79段階</strong>ありました。<br />実際に使われていたのは、<strong>5段階</strong>でした。</p>
				</blockquote>
				<p class="section-lead">
					部門ごとに最適化されたツールとプロセスは、つなぐ人がいないまま増えていきます。
					マーケはMAに、営業はSFAに、CSは別のツールに顧客を持つ。
					同じ会社の同じ顧客なのに、システムの中では別人として扱われている。
				</p>
				<p class="why-stat">
					マーケティングと営業が「非常に良く連携できている」と答えた人は、<strong>13.9%</strong>にとどまります。
					<span class="why-stat__src">Sansan「BtoBマーケティングと営業の連携に関する実態調査」2023年、n=1,000</span>
				</p>
			</div>
			<div class="why-problems">
				<p class="why-problems__label">結果として起きるのは、この3つです。</p>

				<div class="layer-card">
					<h3 class="layer-title">施策の効果が測れない</h3>
					<p class="layer-desc">どの施策の何円が、どの受注になったのかがつながらない。</p>
				</div>

				<div class="layer-card">
					<h3 class="layer-title">商談が、止まった理由がわからないまま消える</h3>
					<p class="layer-desc">稟議で止まったのか、価格で負けたのかが記録に残らない。</p>
				</div>

				<div class="layer-card">
					<h3 class="layer-title">入れたツールが使われなくなる</h3>
					<p class="layer-desc">入力しても返ってくるものがないから、現場は合理的に離脱する。</p>
				</div>

				<p class="why-close">
					これは営業部だけ、マーケ部だけを改善しても直りません。
					<strong>単体の部門では直せない、構造の問題です。</strong>
				</p>
			</div>
		</div>
	</div>
</section>

<!-- ===================== HOW ===================== -->
<section class="how">
	<div class="container">
		<div class="how-grid">
			<div class="how-visual">
				<div class="flow-diagram">
					<div class="flow-step flow-step--top">
						<span class="flow-label">マーケティング</span>
						<div class="flow-tools">MA / CDP</div>
					</div>
					<div class="flow-arrow">↓</div>
					<div class="flow-center">
						<span class="flow-center-label">収益プロセス設計</span>
						<span class="flow-center-sub">Revenue Architecture</span>
					</div>
					<div class="flow-arrow">↓</div>
					<div class="flow-step flow-step--mid">
						<span class="flow-label">営業・インサイドセールス</span>
						<div class="flow-tools">CRM / SFA</div>
					</div>
					<div class="flow-arrow">↓</div>
					<div class="flow-step flow-step--bot">
						<span class="flow-label">カスタマーサクセス</span>
						<div class="flow-tools">CS / BI / データ基盤</div>
					</div>
					<div class="flow-kpi">
						<span>KPI・ダッシュボード</span>
						<span class="flow-kpi-arrow">←</span>
						<span>経営判断に使えるデータへ</span>
					</div>
				</div>
			</div>
			<div class="how-copy">
				<p class="section-eyebrow">Our Approach</p>
				<h2>やることは、<span class="text-primary">3つです。</span></h2>
				<ul class="how-points">
					<li>
						<span class="how-num">01</span>
						<div>
							<strong>つなぐ</strong>
							<p>マーケ・営業・CSに散らばった顧客データを、CRMを軸にひとつに統合します。Salesforce、HubSpot、kintone、Marketo、Zoho、ServiceNow——今お使いのツールを前提に設計します。乗り換えありきではありません。</p>
						</div>
					</li>
					<li>
						<span class="how-num">02</span>
						<div>
							<strong>見えるようにする</strong>
							<p>部門ごとにバラバラだった数字の定義を揃え、経営判断に使えるダッシュボードに落とします。「この数字は何を指しているのか」を、全員が同じように答えられる状態をつくります。</p>
						</div>
					</li>
					<li>
						<span class="how-num">03</span>
						<div>
							<strong>回るようにする</strong>
							<p>営業会議と1on1の運用に組み込み、現場が使い続ける状態まで持っていきます。入力と集計はワークフロー自動化や要約支援に寄せ、人の手を残す場所を減らします。「ちゃんと入力してください」と言い続ける必要がなくなったとき、CRMは初めて機能しています。</p>
						</div>
					</li>
				</ul>
				<a href="/revenue-architecture/" class="btn btn--outline-primary">
					レベニューアーキテクチャとは →
				</a>
			</div>
		</div>
	</div>
</section>

<!-- ===================== AFTER（動き出したあと） ===================== -->
<section class="after">
	<div class="container">
		<header class="section-head after-head">
			<p class="section-eyebrow">After</p>
			<h2>仕組みが動き出すと、<br />こう変わります</h2>
		</header>

		<div class="after-list">

			<article class="after-item">
				<h3>営業会議が、報告会から意思決定の場に変わる</h3>
				<p>
					「誰がいくら」の読み上げに使っていた時間が消え、
					「この案件はなぜ止まっているのか」を話す時間になります。
				</p>
			</article>

			<article class="after-item">
				<h3>「あの件、どうなった？」を聞かなくてよくなる</h3>
				<p>
					聞かないと分からない状態が、見れば分かる状態になります。
					担当者が休んでも、異動しても、案件は前に進み続けます。
				</p>
			</article>

			<article class="after-item after-item--metric">
				<h3>数字をつくる時間が、返ってくる</h3>
				<p>
					ある医療テック企業（約150名）では、データ管理・抽出の作業が
					<strong>約65%</strong>減り、<strong>月に約100時間</strong>が戻ってきました。
					その時間は、戦略に使われています。
				</p>
			</article>

			<article class="after-item after-item--metric">
				<h3>打ち手が、当たったかどうか分かるようになる</h3>
				<p>
					あるITバックオフィスSaaS（約200名）では、マーケが渡したリードのうち
					商談になる割合が<strong>最大20%</strong>上がりました。
					「なぜ商談にならないのか」が、感想ではなくデータで話せるようになったからです。
				</p>
			</article>

			<article class="after-item after-item--metric">
				<h3>人が増えなくても、事業は伸ばせる</h3>
				<p>
					3名の兼務チームで新規プロダクトを立ち上げた会社では、
					<strong>2ヶ月</strong>でGTM戦略を明文化し、3名のままで回り続ける収益エンジンができました。
				</p>
			</article>

		</div>

		<p class="after-close">
			止まっていたものが、動き出す。<br />
			<span>そこまでを、Consilegyの仕事とします。</span>
		</p>
	</div>
</section>

<!-- ===================== ASSURANCE（担保） ===================== -->
<section class="assurance">
	<div class="container">
		<header class="section-head assurance-head">
			<p class="section-eyebrow">Why It Works</p>
			<h2 class="assurance-tagline">
				We don't hand off.<br />We stay until it works.
			</h2>
			<p class="assurance-ja">導入して終わりにしない。成果が出る運用まで伴走する。</p>
			<p class="section-lead">
				戦略レポートも、きれいな設計図も、それ自体は1円も生みません。
				現場が使い始め、使い続けて、初めて数字が動きます。
			</p>
			<p class="section-lead" style="margin-top: 1rem;">
				だからConsilegyは、設計図を渡して終わりにしません。
				「これがないと仕事が回らない」と現場が言う状態まで並走します。
			</p>
		</header>

		<div class="assurance-grid">
			<article class="as-card">
				<span class="as-num">01</span>
				<h3>ダッシュボードの読み方を一緒に設計する</h3>
				<p>
					つくって渡すだけでは見られません。どの数字を、誰が、どの頻度で見て、
					何を判断するのか。運用の側から逆算して設計します。
				</p>
			</article>
			<article class="as-card">
				<span class="as-num">02</span>
				<h3>営業会議での使い方まで決める</h3>
				<p>
					「誰がいくら」の読み上げに戻らないよう、会議のアジェンダと
					見る画面をセットで設計します。会議体が変わらなければ、数字は使われません。
				</p>
			</article>
			<article class="as-card">
				<span class="as-num">03</span>
				<h3>1on1での問いの立て方まで踏み込む</h3>
				<p>
					マネージャーが何を聞くかで、入力の質が決まります。
					データを見ながら案件を前に進める問いを、現場と一緒に言語化します。
				</p>
			</article>
			<article class="as-card">
				<span class="as-num">04</span>
				<h3>売りたいツールを持っていない</h3>
				<p>
					特定ベンダーの販売代理店ではないため、導入本数で収益を得ていません。
					既存のSalesforce・HubSpot・Kintone・Marketoを活かす判断も、
					止める判断も、事業構造だけを基準に出せます。
				</p>
			</article>
		</div>
	</div>
</section>

<!-- ===================== TRACK RECORD（実績） ===================== -->
<section class="track">
	<div class="container">
		<header class="section-head track-head">
			<p class="section-eyebrow">Track Record</p>
			<h2>支援実績 40社。</h2>
			<p class="section-lead">
				代表・山口聖子の個人事業時代からの実績を含め、これまで40社の収益基盤づくりに携わってきました。
				積み上げた知見を、法人として体系化しています。
			</p>
		</header>

		<ul class="track-list">
			<li class="track-item">
				<span class="track-who">医療テック企業<span class="track-size">約150名</span></span>
				<p class="track-result">データ管理・抽出業務を<strong>約65%削減</strong>。<strong>月約100時間</strong>を創出</p>
			</li>
			<li class="track-item">
				<span class="track-who">ITバックオフィスSaaS<span class="track-size">約200名</span></span>
				<p class="track-result">MQL→SQL転換率を<strong>最大20%向上</strong></p>
			</li>
			<li class="track-item">
				<span class="track-who">グローバルDTCブランド<span class="track-size">約20,000名</span></span>
				<p class="track-result">企画から公開までのリードタイムを<strong>約70〜80%短縮</strong>。移行は1ヶ月で完遂</p>
			</li>
			<li class="track-item">
				<span class="track-who">コンサル新規プロダクト部門<span class="track-size">3名の兼務チーム</span></span>
				<p class="track-result"><strong>2ヶ月</strong>でGTM戦略を明文化し、3名でも回る収益エンジンを構築</p>
			</li>
		</ul>

		<p class="track-foot">SaaS、EC / D2C、メーカー、専門サービスまで、業種を問わず支援しています。</p>
		<p class="track-more"><a class="btn btn--secondary" href="/case-studies/">導入事例を見る</a></p>

		<article class="track-media">
			<img
				src="/images/common/seenews-hualien-2026.jpg"
				alt="See News掲載記事のキャプチャ"
				width="1000"
				height="1030"
				loading="lazy"
			/>
			<div>
				<p class="track-media__label">Media</p>
				<h3>海外メディア掲載</h3>
				<p>Nomad Hualien参加時の取り組みが、台湾のSee Newsで紹介されました。</p>
				<a href="https://www.seenews.com.tw/news_data.php?news_id=2325&amp;k_code=A" target="_blank" rel="noopener noreferrer">記事を見る</a>
			</div>
		</article>
	</div>
</section>

<!-- ===================== FOUNDER（誰がやるか） ===================== -->
<section class="founder">
	<div class="container founder-inner">
		<div class="founder-photo">
			<img
				src="/images/common/seiko-yamaguchi-2026.jpg"
				alt="Consilegy 代表 山口 聖子"
				width="480"
				height="600"
				loading="lazy"
			/>
		</div>
		<div class="founder-copy">
			<p class="section-eyebrow">Who Does It</p>
			<h2>設計するのは、<br />代表の山口聖子です。</h2>
			<p class="founder-bio">
				B2Bマーケティング歴10年以上。Moloco、Kaizen Platform、アリババ、UCC上島珈琲、
				西川などで、マーケティング・営業・CRMの設計と実装を担当してきました。
			</p>
			<p class="founder-bio">
				LinkedInで6,400名、ニュースレターで約2,000名に、日本のB2B収益設計について発信しています。
			</p>
			<blockquote class="founder-quote">
				「戦略だけ描いて去る人」を何度も見てきたので、私はそうしないと決めています。
			</blockquote>
			<a href="/company/" class="btn btn--outline-primary">会社概要を見る →</a>
		</div>
	</div>
</section>

<!-- ===================== PRICING（料金） ===================== -->
<section class="pricing">
	<div class="container">
		<header class="section-head pricing-head">
			<p class="section-eyebrow">Pricing</p>
			<h2>料金</h2>
		</header>

		<div class="pricing-block">
			<p class="pricing-for">まだ仕組みがない会社へ</p>
			<div class="price-row price-row--single">
				<span class="price-name">Revenue CRM</span>
				<span class="price-amount">月額 準備中</span>
			</div>
			<p class="pricing-note">※ 現在クローズドβ。数社限定でご提供しています。</p>
			<a href="/crm/" class="pricing-link">β版情報を見る</a>
		</div>

		<div class="pricing-block pricing-block--consulting">
			<p class="pricing-for">
				入れたのに動いていない会社へ
			</p>
			<div class="price-rows">
				<div class="price-row">
					<span class="price-step">Step 01</span>
					<span class="price-name">収益構造の詳細分析</span>
					<span class="price-amount">10万円 〜 50万円</span>
				</div>
				<div class="price-row">
					<span class="price-step">Step 02</span>
					<span class="price-name">収益プロセスの再設計</span>
					<span class="price-amount">300万円 〜 800万円</span>
				</div>
				<div class="price-row">
					<span class="price-step">Step 03</span>
					<span class="price-name">伴走・顧問支援</span>
					<span class="price-amount">月額 30万円 〜 100万円</span>
				</div>
			</div>

			<div class="pricing-guide">
				<p class="pricing-guide__label">どこから始めるべきかは、次の目安で判断できます。</p>
				<ul>
					<li><span>何が問題かまだ言語化できていない</span><em>Step 01</em></li>
					<li><span>課題は見えていて、作り直す判断はできている</span><em>Step 02</em></li>
					<li><span>設計はあるが、現場で回っていない</span><em>Step 03</em></li>
				</ul>
			</div>

			<p class="pricing-diag">
				無料の収益診断（3分）は、この判断そのものをお手伝いするものです。
				<strong>Step 01の詳細分析とは別のもの</strong>で、費用はかかりません。
			</p>
			<a href="/services/" class="pricing-link">料金の詳細を見る →</a>
		</div>
	</div>
</section>

<!-- ===================== INDUSTRIES（業界） ===================== -->
<section class="industries">
	<div class="container">
		<header class="section-head industries-head">
			<p class="section-eyebrow">Industries</p>
			<h2>業界ごとの、詰まりやすい場所</h2>
			<p class="section-lead">業種を問わず支援しています。ここでは、ご相談の多い3つを挙げます。</p>
		</header>
		<div class="ind-grid">
			<article class="ind-card">
				<h3>SaaS の場合</h3>
				<p>
					営業からカスタマーサクセスへの引き継ぎで、契約日は残っていても
					「顧客が何を実現したかったのか」が残っていない——これが解約に直結します。
				</p>
				<p class="ind-stat">
					引き継ぎが不十分な企業は、<strong>63.3%</strong>が解約率2%以上。
					引き継ぎが十分な企業の<strong>1.7倍</strong>です。
					<span class="ind-src">HiCustomer「カスタマーサクセス白書2023」2023年4月18〜30日、n=203</span>
				</p>
				<p>
					複数プロダクト展開時に、営業が慣れた既存プロダクトに戻ってしまう。
					エンタープライズを狙った瞬間、これまでのインバウンド型が通用しなくなる。
					こうした局面の収益設計を支援しています。
				</p>
			</article>
			<article class="ind-card">
				<h3>EC・D2C の場合</h3>
				<p>
					LTVが見えないまま広告費を積んでいる。
					GA4・MA・受注データが別々の場所にあり、
					「このお客様が、いくら使ってくれたのか」を出すのに毎回手作業が発生する。
				</p>
				<p>
					サブスク利用者の<strong>69.4%</strong>が「解約を検討したことがある」と答えています。
					検討理由の1位は「節約のため」。
					<span class="ind-src">ナイル「サブスク利用実態調査」（Appliv調べ）2024年2月2〜7日、n=605</span>
				</p>
				<p>
					価格の問題に見えて、多くは体験と頻度の設計の問題です。
					どのタイミングで何が起きると離脱するのかを、データで特定するところから始めます。
				</p>
			</article>
			<article class="ind-card">
				<h3>メーカー・卸 の場合</h3>
				<p>
					販売チャネルが代理店・EC・直販に分かれ、顧客情報が売り先ごとに分断される。
					「最終顧客が誰なのか」が社内で一致しないまま、施策だけが積み上がります。
				</p>
				<p class="ind-stat">
					データを「全社で利活用している」日本企業は<strong>24.2%</strong>。
					「事業部門・部署ごとに利活用している」の<strong>36.0%</strong>を下回ります。
					<span class="ind-src">IPA「DX動向2025」2025年2月10日〜3月28日、n=1,520</span>
				</p>
				<p>
					展示会の名刺、Webの問い合わせ、既存取引先の窓口が別々に管理され、
					同じ企業に別々のアプローチをしてしまう。
					統合すべきは組織ではなく、顧客を指すIDです。
				</p>
				<p>
					どのチャネル経由の顧客が長く続くのかを出せる状態にしてから、投資配分を決めます。
				</p>
			</article>
		</div>
	</div>
</section>

<!-- ===================== FLAG（旗） ===================== -->
<section class="flag">
	<div class="container flag-inner">
		<div>
			<p class="section-eyebrow">Revenue Architecture</p>
			<h2>私たちはこの設計思想を<br /><span class="text-primary">レベニューアーキテクチャ</span>と呼んでいます</h2>
		</div>
		<div class="flag-body">
			<p>
				海外のB2B SaaSで体系化されてきた考え方を、
				日本の稟議・合意形成のプロセスに合わせて翻案したものです。
			</p>
			<p class="flag-claim">日本の稟議は、待ち時間の塊です。</p>
			<p>
				関わる人が多く、承認が積み上がる。だからConsilegyの設計では、
				転換率だけでなく<strong>「待ち時間」も指標に入れます</strong>。
				商談が止まった場所と、止まっていた長さが記録に残る構造にします。
			</p>
			<a href="/revenue-architecture/" class="btn btn--outline-primary">レベニューアーキテクチャとは →</a>
		</div>
	</div>
</section>

<!-- ===================== BLOG ===================== -->
<section class="blog-section">
	<div class="container">
		<header class="section-head">
			<p class="section-eyebrow">Insights</p>
			<h2>最新コラム</h2>
			<p class="section-lead">RevOps・CRM運用・データ活用に関するナレッジを公開しています。</p>
		</header>
		<div class="blog-grid">
			{insightsPosts.map((post) => (
				<article class="blog-card">
					<a href={`/insights/${post.slug}/`} class="blog-card__link">
						<div class="blog-img">
							<img
								src={`/images/insights/${post.slug}.svg`}
								alt={post.category}
								loading="lazy"
								width="1200"
								height="630"
								class="blog-img__asset"
							/>
							<span class="blog-new">新着</span>
						</div>
						<div class="blog-body">
							<span class="blog-cat">{post.category}</span>
							<h3 class="blog-title">{post.title}</h3>
							<p class="blog-desc">{post.description}</p>
							<div class="blog-foot">
								<span class="blog-arrow" aria-hidden="true">→</span>
							</div>
						</div>
					</a>
				</article>
			))}
		</div>
		<p style="margin-top: var(--space-lg); text-align: center;">
			<a class="btn btn--secondary" href="/insights/">コラム一覧へ</a>
		</p>
	</div>
</section>

<!-- ===================== NEWSLETTER CTA ===================== -->
<section class="nl-cta-strip">
	<div class="container nl-cta-strip__inner">
		<div class="nl-cta-strip__copy">
			<p class="nl-cta-strip__eyebrow">Newsletter</p>
			<h2>次世代B2Bレベニュー戦略バイブル</h2>
			<p>Revenue Architecture・RevOps・CRM実装の実践知を毎月配信。約2,000人が購読中。</p>
		</div>
		<a href="/newsletter/#signup" class="btn nl-cta-strip__btn">
			無料で登録する →
		</a>
	</div>
</section>

<!-- ===================== FINAL CTA ===================== -->
<section class="final-cta">
	<div class="container cta-inner">
		<div class="cta-copy">
			<p class="cta-eyebrow">Get Started</p>
			<h2>まず、どこで<br />取りこぼしているかを見る</h2>
			<p class="cta-lead">
				収益診断は無料です。3分で終わります。
				全12問・選択式で、6つの「境目」のどこで売上が漏れているか、
				何から手をつけるべきかを整理してお返しします。
			</p>
			<p class="cta-lead cta-lead--close">
				診断は、質問が価値ではありません。結果が価値です。<br />
				<strong>止まっている場所さえ分かれば、事業は動き出します。</strong>
			</p>
			<div class="cta-actions">
				<a href="/diagnostic/" class="btn btn--cta-primary btn--lg">
					3分の収益診断を受ける（無料）
				</a>
				<a href="/download/" class="btn btn--cta-outline btn--lg">
					サービス資料をダウンロード
				</a>
			</div>
		</div>
		<div class="cta-checklist">
			<h4>診断でわかること</h4>
			<ul>
				<li>売上成長を止めている最大のボトルネック</li>
				<li>6つの「境目」それぞれの状態（赤・黄・緑）</li>
				<li>漏れの重なりから見える「取りこぼしの型」</li>
				<li>優先順に並んだ推奨施策</li>
				<li>今日から着手できる最初の一手</li>
			</ul>
		</div>
	</div>
</section>

</BaseLayout>

<style>
	.container {
		max-width: 1160px;
		margin-inline: auto;
		padding-inline: 2rem;
	}

	section {
		padding-block: var(--space-2xl);
	}

	h2,
	h3 {
		letter-spacing: 0;
	}

	.text-primary {
		color: var(--color-primary);
	}

	.section-eyebrow,
	.hero-kicker,
	.cta-eyebrow,
	.nl-cta-strip__eyebrow {
		display: block;
		margin: 0;
		color: var(--color-text-muted);
		font-size: 0.75rem;
		font-weight: 600;
		letter-spacing: 0.12em;
		line-height: 1.4;
		text-transform: uppercase;
	}

	.section-head,
	.fork-head {
		display: grid;
		grid-template-columns: 12rem minmax(0, 1fr);
		gap: var(--space-lg);
		align-items: start;
		max-width: none;
		margin-bottom: var(--space-lg);
		padding-top: var(--space-sm);
		border-top: 1px solid var(--color-border);
	}

	.section-head > :not(.section-eyebrow),
	.fork-head > :not(.section-eyebrow) {
		grid-column: 2;
	}

	.section-head > .section-eyebrow,
	.fork-head > .section-eyebrow {
		grid-column: 1;
		grid-row: 1 / span 20;
	}

	.section-head h2,
	.fork-head h2,
	.why-head h2,
	.how-copy h2,
	.founder-copy h2,
	.flag-inner h2,
	.cta-copy h2 {
		margin: 0;
		color: var(--color-text);
		font-size: 2rem;
		font-weight: 500;
		line-height: 1.4;
		text-wrap: balance;
	}

	.section-lead,
	.founder-bio,
	.flag-body p,
	.cta-lead {
		margin: 0;
		color: var(--color-text-muted);
		font-size: 0.9375rem;
		line-height: 1.85;
	}

	.btn,
	.btn--hero-primary,
	.btn--hero-outline,
	.btn--outline-primary,
	.btn--cta-primary,
	.btn--cta-outline,
	.nl-cta-strip__btn,
	.fork-third__btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 46px;
		padding: 0.8rem 1.35rem;
		border: 1px solid var(--color-border);
		border-radius: 4px;
		font-size: 0.9375rem;
		font-weight: 600;
		line-height: 1.25;
		text-decoration: none;
		transition: background 0.15s ease, color 0.15s ease, border-color 0.15s ease;
	}

	.btn--hero-primary,
	.btn--cta-primary,
	.nl-cta-strip__btn,
	.fork-third__btn {
		background: var(--color-primary);
		border-color: var(--color-primary);
		color: #fff;
	}

	.btn--hero-primary:hover,
	.btn--cta-primary:hover,
	.nl-cta-strip__btn:hover,
	.fork-third__btn:hover {
		background: var(--color-primary-dark);
		border-color: var(--color-primary-dark);
		color: #fff;
	}

	.btn--hero-outline,
	.btn--outline-primary,
	.btn--cta-outline,
	.btn--secondary {
		background: transparent;
		color: var(--color-text);
	}

	.btn--hero-outline:hover,
	.btn--outline-primary:hover,
	.btn--cta-outline:hover,
	.btn--secondary:hover {
		border-color: var(--color-primary);
		color: var(--color-primary);
	}

	.hero {
		background: var(--color-bg);
		border-bottom: 1px solid var(--color-border);
		padding-block: 0;
	}

	.hero-inner {
		display: grid;
		grid-template-columns: minmax(0, 1fr) minmax(320px, 360px);
		gap: var(--space-lg);
		align-items: start;
		padding-block: 3.5rem 2.75rem;
	}

	.hero-kicker {
		margin-bottom: var(--space-md);
		padding-top: 0.875rem;
		border-top: 1px solid var(--color-border);
	}

	.hero-copy h1 {
		max-width: 14.5em;
		margin: 0;
		color: var(--color-text);
		font-size: 3rem;
		font-weight: 500;
		line-height: 1.16;
		letter-spacing: 0;
		text-wrap: balance;
	}

	.hero-accent {
		display: block;
		color: var(--color-text);
	}

	.hero-symptoms {
		display: grid;
		gap: 0;
		margin: 1.25rem 0 0;
		padding: 0;
		list-style: none;
		border-top: 1px solid var(--color-border);
		border-bottom: 1px solid var(--color-border);
	}

	.hero-symptoms li {
		padding: 0.55rem 0;
		border-bottom: 1px solid var(--color-border);
		color: var(--color-text);
		font-size: 0.9375rem;
		line-height: 1.7;
	}

	.hero-symptoms li:last-child {
		border-bottom: 0;
	}

	.hero-lead {
		max-width: 44em;
		margin: 1.1rem 0 0;
		color: var(--color-text-muted);
		font-size: 0.9375rem;
		line-height: 1.75;
	}

	.hero-actions,
	.cta-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
		margin-top: 1.15rem;
	}

	.hero-proof {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 0;
		margin: 1rem 0 0;
		padding: 0;
		list-style: none;
		border-top: 1px solid var(--color-border);
		border-bottom: 1px solid var(--color-border);
	}

	.hero-proof li {
		min-width: 0;
		padding: 0.65rem 0.85rem;
		color: var(--color-text-muted);
		font-size: 0.8125rem;
		line-height: 1.6;
	}

	.hero-proof li:first-child {
		padding-left: 0;
	}

	.hero-proof li + li {
		border-left: 1px solid var(--color-border);
	}

	.hero-proof a,
	.fork-link,
	.pricing-link {
		color: var(--color-primary);
		font-weight: 600;
		text-decoration: none;
		border-bottom: 1px solid currentColor;
	}

	.hero-media {
		min-width: 0;
		margin: 0;
	}

	.track-media__label {
		display: block;
		color: var(--color-text-muted);
		font-size: 0.6875rem;
		font-weight: 700;
		letter-spacing: 0.12em;
		line-height: 1.5;
		text-transform: uppercase;
	}

	.hero-media img,
	.founder-photo img,
	.blog-img__asset {
		display: block;
		width: 100%;
		height: auto;
	}

	.hero-media img,
	.founder-photo img {
		border: 1px solid var(--color-border);
		border-radius: 0;
		background: #fff;
	}

	.hero-media figcaption {
		margin-top: 0.55rem;
		color: var(--color-text-muted);
		font-size: 0.75rem;
		line-height: 1.6;
	}

	.hero-tools {
		background: var(--color-bg-subtle);
		border-top: 1px solid var(--color-border);
		padding: 1rem 0;
	}

	.tools-label {
		margin: 0 0 0.5rem;
		color: var(--color-text-muted);
		font-size: 0.75rem;
		font-weight: 600;
		letter-spacing: 0.08em;
		line-height: 1.4;
	}

	.tools-row {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 0.25rem 0.625rem;
		color: var(--color-text);
		font-size: 0.8125rem;
		line-height: 1.7;
	}

	.tools-sep {
		color: var(--color-border);
	}

	.fork,
	.how,
	.assurance,
	.founder,
	.industries,
	.nl-cta-strip {
		background: var(--color-bg-subtle);
		border-top: 1px solid var(--color-border);
		border-bottom: 1px solid var(--color-border);
	}

	.why,
	.after,
	.track,
	.pricing,
	.flag,
	.blog-section,
	.final-cta {
		background: var(--color-bg);
		border-top: 1px solid var(--color-border);
	}

	.fork-grid,
	.after-list,
	.assurance-grid,
	.ind-grid,
	.blog-grid {
		display: grid;
		gap: 0;
		border-top: 1px solid var(--color-border);
	}

	.fork-grid {
		grid-template-columns: repeat(2, minmax(0, 1fr));
	}

	.fork-card,
	.after-item,
	.as-card,
	.ind-card,
	.blog-card {
		min-width: 0;
		background: transparent;
		border: 0;
		border-bottom: 1px solid var(--color-border);
		border-radius: 0;
	}

	.fork-card {
		display: flex;
		flex-direction: column;
		padding: 1.5rem var(--space-lg) 1.5rem 0;
	}

	.fork-card:nth-child(2) {
		padding-left: var(--space-lg);
		padding-right: 0;
		border-left: 1px solid var(--color-border);
	}

	.fork-tag,
	.pricing-for,
	.blog-cat,
	.price-step {
		color: var(--color-primary);
		font-size: 0.75rem;
		font-weight: 700;
		letter-spacing: 0.08em;
		line-height: 1.5;
	}

	.fork-tag {
		display: block;
		margin-bottom: 0.9rem;
	}

	.fork-state {
		margin: 0 0 1rem;
		color: var(--color-text);
		font-size: 1.0625rem;
		font-weight: 600;
		line-height: 1.75;
	}

	.fork-body {
		margin: 0 0 0.85rem;
		color: var(--color-text-muted);
		font-size: 0.9375rem;
		line-height: 1.85;
	}

	.fork-body strong,
	.ind-stat strong,
	.track-result strong,
	.after-item strong,
	.why-stat strong {
		color: var(--color-primary);
		font-weight: 600;
	}

	.fork-src,
	.ind-src,
	.why-stat__src {
		display: block;
		margin-top: 0.5rem;
		color: var(--color-text-muted);
		font-size: 0.75rem;
		line-height: 1.65;
	}

	.fork-links {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem 1rem;
		align-items: center;
		margin-top: auto;
		padding-top: 0.5rem;
	}

	.fork-link {
		display: inline-flex;
		font-size: 0.9375rem;
	}

	.fork-third {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
		margin-top: var(--space-lg);
		padding-top: var(--space-sm);
		border-top: 1px solid var(--color-border);
	}

	.fork-third p {
		max-width: 46em;
		margin: 0;
		color: var(--color-text-muted);
		font-size: 0.9375rem;
		line-height: 1.8;
	}

	.why-grid,
	.how-grid,
	.founder-inner,
	.flag-inner,
	.cta-inner {
		display: grid;
		grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
		gap: var(--space-xl);
		align-items: start;
	}

	.why-head,
	.how-copy,
	.flag-inner > div:first-child,
	.cta-copy {
		padding-top: var(--space-sm);
		border-top: 1px solid var(--color-border);
	}

	.why-head .section-eyebrow,
	.how-copy .section-eyebrow,
	.founder-copy .section-eyebrow,
	.flag-inner .section-eyebrow,
	.cta-eyebrow {
		margin-bottom: var(--space-sm);
	}

	.why-claim {
		margin: var(--space-md) 0;
		color: var(--color-text);
		font-size: 1.0625rem;
		font-weight: 600;
		line-height: 1.8;
	}

	.why-anecdote,
	.founder-quote,
	.ind-stat,
	.pricing-diag,
	.flag-claim {
		margin: var(--space-md) 0;
		padding: 0.95rem 1rem;
		background: #fff;
		border: 1px solid var(--color-border);
		border-left: 3px solid var(--color-primary);
		border-radius: 0 4px 4px 0;
	}

	.why-anecdote p,
	.founder-quote {
		color: var(--color-text);
		font-size: 0.9375rem;
		line-height: 1.8;
	}

	.why-stat {
		margin: var(--space-md) 0 0;
		padding-top: var(--space-md);
		border-top: 1px solid var(--color-border);
		color: var(--color-text-muted);
		font-size: 0.9375rem;
		line-height: 1.8;
	}

	.why-problems {
		border-top: 1px solid var(--color-border);
	}

	.why-problems__label {
		margin: 0;
		padding: 0.95rem 0;
		border-bottom: 1px solid var(--color-border);
		color: var(--color-text-muted);
		font-size: 0.8125rem;
		font-weight: 600;
		line-height: 1.7;
	}

	.layer-card {
		padding: 1.05rem 0;
		border-bottom: 1px solid var(--color-border);
	}

	.layer-title {
		margin: 0 0 0.45rem;
		color: var(--color-text);
		font-size: 1rem;
		font-weight: 600;
		line-height: 1.55;
	}

	.layer-desc,
	.why-close,
	.after-item p,
	.as-card p,
	.ind-card p,
	.pricing-note,
	.pricing-diag,
	.blog-desc,
	.nl-cta-strip__copy p,
	.track-foot {
		margin: 0;
		color: var(--color-text-muted);
		font-size: 0.9375rem;
		line-height: 1.85;
	}

	.why-close {
		margin-top: var(--space-md);
	}

	.why-close strong {
		color: var(--color-text);
		font-weight: 600;
	}

	.flow-diagram {
		display: flex;
		flex-direction: column;
		gap: 0.55rem;
		padding: var(--space-md);
		background: #fff;
		border: 1px solid var(--color-border);
		border-radius: 4px;
	}

	.flow-step,
	.flow-center,
	.flow-kpi {
		width: 100%;
		padding: 0.85rem 1rem;
		background: var(--color-bg);
		border: 1px solid var(--color-border);
		border-radius: 4px;
	}

	.flow-center {
		background: var(--color-primary);
		border-color: var(--color-primary);
		text-align: center;
	}

	.flow-label,
	.flow-center-sub {
		display: block;
		color: var(--color-text-muted);
		font-size: 0.75rem;
		font-weight: 600;
		letter-spacing: 0.08em;
		line-height: 1.5;
	}

	.flow-tools,
	.flow-center-label {
		display: block;
		margin-top: 0.2rem;
		color: var(--color-text);
		font-size: 0.9375rem;
		font-weight: 600;
		line-height: 1.6;
	}

	.flow-center-label,
	.flow-center-sub {
		color: #fff;
	}

	.flow-center-sub {
		opacity: 0.72;
	}

	.flow-arrow {
		color: var(--color-primary);
		font-size: 1rem;
		line-height: 1;
		text-align: center;
	}

	.flow-kpi {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 0.5rem;
		color: var(--color-text-muted);
		font-size: 0.8125rem;
		line-height: 1.6;
	}

	.how-points {
		display: flex;
		flex-direction: column;
		gap: 0;
		margin: var(--space-md) 0 var(--space-lg);
		padding: 0;
		list-style: none;
		border-top: 1px solid var(--color-border);
	}

	.how-points li {
		display: grid;
		grid-template-columns: 4rem minmax(0, 1fr);
		gap: var(--space-md);
		padding: 1.1rem 0;
		border-bottom: 1px solid var(--color-border);
	}

	.how-num,
	.as-num {
		color: rgba(30, 64, 175, 0.24);
		font-size: 2.45rem;
		font-weight: 500;
		font-variant-numeric: tabular-nums;
		line-height: 0.9;
		letter-spacing: 0;
	}

	.how-points strong {
		display: block;
		margin-bottom: 0.35rem;
		color: var(--color-text);
		font-size: 1rem;
		font-weight: 600;
		line-height: 1.5;
	}

	.how-points p {
		margin: 0;
		color: var(--color-text-muted);
		font-size: 0.9375rem;
		line-height: 1.85;
	}

	.after-list,
	.assurance-grid {
		grid-template-columns: repeat(2, minmax(0, 1fr));
	}

	.after-item,
	.as-card {
		padding: 1.35rem var(--space-md) 1.35rem 0;
	}

	.after-item:nth-child(even),
	.as-card:nth-child(even) {
		padding-left: var(--space-md);
		border-left: 1px solid var(--color-border);
	}

	.after-item h3,
	.as-card h3,
	.ind-card h3 {
		margin: 0 0 0.6rem;
		color: var(--color-text);
		font-size: 1rem;
		font-weight: 600;
		line-height: 1.55;
	}

	.after-close {
		margin: var(--space-lg) 0 0;
		padding-top: var(--space-sm);
		border-top: 1px solid var(--color-border);
		color: var(--color-text);
		font-size: 1.375rem;
		font-weight: 500;
		line-height: 1.6;
		text-align: center;
	}

	.after-close span {
		color: var(--color-text-muted);
		font-size: 0.9375rem;
		font-weight: 400;
	}

	.assurance-tagline {
		color: var(--color-primary) !important;
		font-size: 2rem !important;
	}

	.assurance-ja {
		margin: var(--space-sm) 0 var(--space-md);
		color: var(--color-text);
		font-size: 1rem;
		font-weight: 600;
		line-height: 1.7;
	}

	.track-list {
		margin: 0;
		padding: 0;
		list-style: none;
		border-top: 1px solid var(--color-border);
	}

	.track-item {
		display: grid;
		grid-template-columns: 320px minmax(0, 1fr);
		gap: var(--space-lg);
		align-items: baseline;
		padding: 1rem 0;
		border-bottom: 1px solid var(--color-border);
	}

	.track-who {
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
		color: var(--color-text);
		font-size: 0.9375rem;
		font-weight: 600;
		line-height: 1.6;
	}

	.track-size {
		color: var(--color-text-muted);
		font-size: 0.75rem;
		font-weight: 400;
	}

	.track-result {
		margin: 0;
		color: var(--color-text-muted);
		font-size: 0.9375rem;
		line-height: 1.75;
	}

	.track-foot,
	.track-more {
		margin-top: var(--space-md);
	}

	.track-media {
		display: grid;
		grid-template-columns: minmax(160px, 260px) minmax(0, 1fr);
		gap: var(--space-md);
		align-items: start;
		margin-top: var(--space-lg);
		padding-top: var(--space-md);
		border-top: 1px solid var(--color-border);
	}

	.track-media img {
		display: block;
		width: 100%;
		height: auto;
		border: 1px solid var(--color-border);
	}

	.track-media h3 {
		margin: 0.35rem 0 0.5rem;
		color: var(--color-text);
		font-size: 1rem;
		font-weight: 600;
		line-height: 1.55;
	}

	.track-media p {
		margin: 0;
		color: var(--color-text-muted);
		font-size: 0.9375rem;
		line-height: 1.75;
	}

	.track-media a {
		display: inline-flex;
		margin-top: 0.85rem;
		color: var(--color-primary);
		font-size: 0.9375rem;
		font-weight: 600;
		text-decoration: none;
		border-bottom: 1px solid currentColor;
	}

	.founder-inner {
		grid-template-columns: minmax(220px, 0.55fr) minmax(0, 1.2fr);
		align-items: center;
	}

	.founder-photo {
		margin: 0;
	}

	.founder-photo img {
		aspect-ratio: 4 / 5;
		object-fit: cover;
	}

	.founder-copy {
		padding-top: var(--space-sm);
		border-top: 1px solid var(--color-border);
	}

	.founder-bio + .founder-bio {
		margin-top: 1rem;
	}

	.founder-quote {
		color: var(--color-text);
		font-weight: 600;
	}

	.pricing-block {
		margin-bottom: var(--space-md);
		padding: var(--space-md) 0 0;
		border-top: 1px solid var(--color-border);
	}

	.pricing-for {
		display: flex;
		flex-wrap: wrap;
		gap: 0.4rem 0.85rem;
		align-items: baseline;
		margin: 0 0 1rem;
	}

	.pricing-for__note {
		color: var(--color-text-muted);
		font-size: 0.8125rem;
		font-weight: 400;
		letter-spacing: 0;
	}

	.price-rows {
		border-top: 1px solid var(--color-border);
	}

	.price-row {
		display: grid;
		grid-template-columns: 6rem minmax(0, 1fr) auto;
		gap: var(--space-md);
		align-items: baseline;
		padding: 0.85rem 0;
		border-bottom: 1px solid var(--color-border);
	}

	.price-row--single {
		grid-template-columns: minmax(0, 1fr) auto;
		border-top: 1px solid var(--color-border);
	}

	.price-name,
	.price-amount {
		color: var(--color-text);
		font-size: 0.9375rem;
		line-height: 1.7;
	}

	.price-amount {
		font-weight: 600;
		white-space: nowrap;
	}

	.pricing-guide {
		margin-top: var(--space-md);
		padding-top: var(--space-md);
		border-top: 1px solid var(--color-border);
	}

	.pricing-guide__label {
		margin: 0 0 0.75rem;
		color: var(--color-text-muted);
		font-size: 0.875rem;
		font-weight: 600;
		line-height: 1.7;
	}

	.pricing-guide ul {
		margin: 0;
		padding: 0;
		list-style: none;
		border-top: 1px solid var(--color-border);
	}

	.pricing-guide li {
		display: grid;
		grid-template-columns: minmax(0, 1fr) 5rem;
		gap: var(--space-md);
		padding: 0.75rem 0;
		border-bottom: 1px solid var(--color-border);
		color: var(--color-text-muted);
		font-size: 0.875rem;
		line-height: 1.7;
	}

	.pricing-guide em {
		color: var(--color-primary);
		font-style: normal;
		font-weight: 600;
		text-align: right;
	}

	.pricing-diag {
		margin-top: var(--space-md);
	}

	.pricing-link {
		display: inline-flex;
		margin-top: var(--space-md);
		font-size: 0.9375rem;
	}

	.ind-grid {
		grid-template-columns: repeat(3, minmax(0, 1fr));
	}

	.ind-card {
		padding: 1.35rem var(--space-md) 1.35rem 0;
	}

	.ind-card:nth-child(2),
	.ind-card:nth-child(3) {
		padding-left: var(--space-md);
		border-left: 1px solid var(--color-border);
	}

	.ind-card p + p {
		margin-top: 0.85rem;
	}

	.flag-claim {
		color: var(--color-text) !important;
		font-size: 1.0625rem !important;
		font-weight: 600;
	}

	.flag-body .btn--outline-primary {
		margin-top: 0.5rem;
	}

	.blog-grid {
		grid-template-columns: repeat(3, minmax(0, 1fr));
	}

	.blog-card {
		background: #fff;
		border: 1px solid var(--color-border);
		border-radius: 4px;
		overflow: hidden;
	}

	.blog-card__link {
		display: flex;
		flex-direction: column;
		height: 100%;
		color: inherit;
		text-decoration: none;
	}

	.blog-img {
		position: relative;
		aspect-ratio: 1200 / 630;
		overflow: hidden;
		background: #fff;
		border-bottom: 1px solid var(--color-border);
	}

	.blog-img__asset {
		height: 100%;
		object-fit: cover;
	}

	.blog-new {
		position: absolute;
		top: 10px;
		right: 10px;
		padding: 0.2rem 0.55rem;
		background: #fff;
		border: 1px solid var(--color-border);
		border-radius: 4px;
		color: var(--color-primary);
		font-size: 0.6875rem;
		font-weight: 700;
		line-height: 1.4;
	}

	.blog-body {
		display: flex;
		flex: 1;
		flex-direction: column;
		padding: 1.15rem;
	}

	.blog-cat {
		display: inline-flex;
		margin-bottom: 0.55rem;
	}

	.blog-title {
		margin: 0 0 0.65rem;
		color: var(--color-text);
		font-size: 0.9375rem;
		font-weight: 600;
		line-height: 1.6;
	}

	.blog-foot {
		margin-top: auto;
		padding-top: 0.75rem;
		border-top: 1px solid var(--color-border);
		text-align: right;
	}

	.blog-arrow {
		color: var(--color-primary);
		font-size: 0.9375rem;
	}

	.nl-cta-strip {
		padding-block: var(--space-lg);
	}

	.nl-cta-strip__inner {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		justify-content: space-between;
		gap: var(--space-lg);
	}

	.nl-cta-strip__copy {
		max-width: 680px;
		padding-top: var(--space-sm);
		border-top: 1px solid var(--color-border);
	}

	.nl-cta-strip__eyebrow {
		margin-bottom: var(--space-sm);
	}

	.nl-cta-strip__copy h2 {
		margin: 0 0 0.6rem;
		color: var(--color-text);
		font-size: 1.75rem;
		font-weight: 500;
		line-height: 1.35;
	}

	.cta-lead + .cta-lead {
		margin-top: 1rem;
	}

	.cta-lead--close {
		margin-bottom: 0;
	}

	.cta-lead--close strong {
		color: var(--color-text);
		font-weight: 600;
	}

	.cta-checklist {
		border-top: 1px solid var(--color-border);
	}

	.cta-checklist h4 {
		margin: 0;
		padding: 0.95rem 0;
		border-bottom: 1px solid var(--color-border);
		color: var(--color-primary);
		font-size: 0.8125rem;
		font-weight: 700;
		letter-spacing: 0.08em;
		line-height: 1.5;
	}

	.cta-checklist ul {
		margin: 0;
		padding: 0;
		list-style: none;
	}

	.cta-checklist li {
		padding: 0.75rem 0;
		border-bottom: 1px solid var(--color-border);
		color: var(--color-text);
		font-size: 0.9375rem;
		line-height: 1.75;
	}

	@media (max-width: 1020px) {
		.hero-inner,
		.why-grid,
		.how-grid,
		.founder-inner,
		.flag-inner,
		.cta-inner {
			grid-template-columns: 1fr;
		}

		.hero-copy h1 {
			font-size: 2.75rem;
		}

		.hero-media {
			max-width: 680px;
		}

		.fork-grid,
		.ind-grid,
		.blog-grid {
			grid-template-columns: 1fr;
		}

		.fork-card:nth-child(2),
		.ind-card:nth-child(2),
		.ind-card:nth-child(3) {
			padding-left: 0;
			padding-right: 0;
			border-left: 0;
		}

		.fork-card,
		.ind-card {
			padding-right: 0;
		}
	}

	@media (max-width: 760px) {
		.container {
			padding-inline: 1.5rem;
		}

		section,
		.hero-inner {
			padding-block: var(--space-xl);
		}

		.section-head,
		.fork-head,
		.after-list,
		.assurance-grid {
			grid-template-columns: 1fr;
		}

		.section-head,
		.fork-head {
			gap: 0.75rem;
		}

		.section-head > :not(.section-eyebrow),
		.fork-head > :not(.section-eyebrow),
		.section-head > .section-eyebrow,
		.fork-head > .section-eyebrow {
			grid-column: auto;
			grid-row: auto;
		}

		.section-head h2,
		.fork-head h2,
		.why-head h2,
		.how-copy h2,
		.founder-copy h2,
		.flag-inner h2,
		.cta-copy h2,
		.assurance-tagline {
			font-size: 1.625rem !important;
		}

		.hero-copy h1 {
			font-size: 2.25rem;
		}

		.hero-proof {
			grid-template-columns: 1fr;
		}

		.hero-proof li {
			padding-inline: 0;
		}

		.hero-proof li + li {
			border-left: 0;
			border-top: 1px solid var(--color-border);
		}

		.after-item,
		.after-item:nth-child(even),
		.as-card,
		.as-card:nth-child(even) {
			padding-left: 0;
			padding-right: 0;
			border-left: 0;
		}

		.track-item,
		.track-media,
		.price-row,
		.price-row--single,
		.pricing-guide li,
		.how-points li {
			grid-template-columns: 1fr;
			gap: 0.35rem;
		}

		.price-amount,
		.pricing-guide em {
			text-align: left;
			white-space: normal;
		}

		.hero-actions,
		.cta-actions {
			flex-direction: column;
			align-items: stretch;
		}
	}
</style>
