# 作業ログ 2026-08-21（持ち越し2・3・4の消化＋英語記事の句読点修復）

**対象**: `docs/worklog-2026-08-19-20.md` の「未対応」2〜4。1（digital-nomad / website-for-events を残すか）は事業判断のため保留。

---

## 4. 診断と無料相談の2本立て → **維持で確定**

削除を検討したが、市場調査の結果、削るほうが明確にマイナスと判断した。

| 調べたこと | 数字 |
|---|---|
| アセスメント/ROI計算ツールのopt-in率 | 30〜50% |
| B2B診断クイズのCVR | 35〜45%（B2B・コンサルで最高水準） |
| クイズ完了率（1億人超のデータ） | 65% |
| B2Bサイト全体のCVR中央値 | 2.9% |

**決め手**: 自己診断を触った人は、記事だけ読んだ人の**2〜3倍**の率でデモ・相談に進む。診断は無料相談の競合ではなく供給源。

「CTAが多いとCVRが下がる」（単一ページで-266% / B2Bで最大-65%、Unbounce 41,000ページ）は**1ページ内で競合する場合**の話で、ファネル段階の違う別ページには当てはまらない。同じゴールのCTAを複数配置するのは逆に+20〜30%。

日本のB2Bは検討期間1〜2年・複数関与者で、情報収集段階からいきなり商談には移らない。WACAは「無料相談はこちら」が機能しない主因として**全ページに同じCTAしかないこと**を挙げ、段階別CTAを推奨している。競合の実態も才流がCVポイント4種（イベント含め10以上）、SEデザインが1ページ内3段階。**2本立ては日本のB2Bコンサルとしてはむしろ少ない。**

**実装は既にベストプラクティス通りだった**（追加作業なし）:
- `/diagnostic/` 参照 43ファイル / `/free-consultation/` 参照 6ファイル → 主従が明確
- 両方が載るページは `/diagnostic/` の1枚だけ（診断2・相談1）
- 1ページに競合CTAを並べている箇所はゼロ

## 3. 未レンダリングの旧md 29本を削除

`src/content/pages/` の35本のうち、実際に出力されていたのは6本だけだった。

| 死んでいた理由 | 本数 |
|---|---|
| 専用の `.astro` ルートが優先される | 6 |
| `[...slug].astro` の除外リスト（exact） | 9 |
| 同（prefix: `services/`, `en/services-en/` 等） | 14 |

**808KB / 8,681行**を削除。残したのは `ga4-utilization-assessment` `ga4-utilization-check` `ga4-utilization-checklist` `ir` `koukoku` `note` の6本。

- `services/digital-nomad` と `services/website-for-events` の**実体は `src/content/services/` 側**。`src/content/pages/` にあったのは旧WPの残骸で、持ち越し1の判断とは無関係
- **検証**: 削除前後でビルド出力を全ファイルハッシュ比較 → 160ページ・709ファイルがバイト一致。差分は sitemap の `lastmod`（毎ビルド変わる）のみで、URL は159本とも同一

## 英語記事の句読点修復（今回の主眼）

前回の「emダッシュ全廃・410箇所」が機械的すぎて、**文が壊れていた**。ダッシュを戻すのではなく、コロン・丸括弧・文分割で復旧した（ダッシュ不使用のルールは維持）。

### 内訳（計77箇所 / 20本）

| 種類 | 件数 | 直し方 |
|---|---|---|
| 対のダッシュ（挿入句）がカンマ化 | 44 | **丸括弧**に。挿入句がリストを含むと文が解析不能になっていた |
| コンマスプライス | 13 | コロンまたは文分割 |
| 見出しの `Label — Sub` | 12 | **コロン**に（対比の `not X` はカンマのまま） |
| その他（リスト誘導・引用符の後の読点） | 8 | コロン / 読点削除 |

**壊れていた例**:
- `A connected data foundation: CRM..., website..., pipeline..., eliminates the aggregation work` → コロンが閉じず主語が迷子 → `A connected data foundation (CRM..., website..., pipeline...) eliminates ...`
- `They inherit global defaults: Subscriber, Lead, MQL, SQL, Opportunity, Customer, configure them in the CRM` → `... global defaults (Subscriber, Lead, MQL, SQL, Opportunity, Customer), configure them in the CRM`
- `What changed was the messaging design, the sequence, the proof type, and the conversion path were rebuilt` → 主述が崩壊 → `... the messaging design: the sequence, ...`
- `Is your Japan conversion path the same as global, same CTA structure, ...` → 対比が列挙に読める → `... the same as global: same CTA structure, ...`
- `The creative was strong, it had performed in other markets.` → `The creative was strong: it had performed in other markets.`

### 取りこぼしていたemダッシュ

`en/crm/school/index.astro` の `demo-caption` に1箇所残っていた（→コロン）。`en/index.astro`・`en/diagnostic/start/index.astro` の6箇所はコード/HTMLコメントなので非公開・そのまま。

### 冗長表現は問題なし

`actually`(55) は「思い込み vs 実際」の対比で機能しているので残した。`needs to be`(14) `in terms of`(4) `leverage`(3・名詞用法) `very`(2) も正当な用法。**英語記事の実害は冗長さではなく句読点だった。**

## 2. 日本語記事24本の読点・冗長さ

|  | 読点総数 | 3読点以上の文 | 4読点以上の文 |
|---|---|---|---|
| 前 | 1,498 | 64 | 18 |
| 後 | **1,441** | **48** | **0** |

- **列挙→中黒 14箇所**: `業種、企業規模、役職、既存ツール、困っている業務、導入時期` → 中黒。`展示会、広告、LP、メール配信、営業資料、商談対応` → 中黒
- **多重読点の整理 8箇所**: 文分割、引用内の読点を句点に、二重に区切っていた箇所を整理
- **冗長表現 9箇所**: `ことになります`×4 → `します`／`という形で`×2／`ていきます`×2／`ということです`×1
- 主語・述語のペア（`マーケティングはリード数、営業は受注金額、CSは継続率を見る`）は読点が正しいので触っていない
- 広めに再スキャンしても `することができ` `ということが` `非常に` `しっかり` などは**0件**。日本語記事の冗長さは前回時点で既に潰れていた

---

## 検証

- `npm run build` 全パス（160ページ）
- 削除は前後のdist全ファイルハッシュ比較で出力不変を確認
- 置換はすべて「文字列が1回だけ一致すること」を検査してから適用（77+31箇所すべて1/1で一致）
- emダッシュの再混入なし

## 未対応（引き続き持ち越し）

1. `/services/digital-nomad/` と `/services/website-for-events/` を残すか（事業判断）
2. 購入前FAQの未決5問（`src/data/crm-faq.ts` 冒頭）。契約期間と解約条件が最優先

---

# 追記：トーン調整と新規記事5本（同日）

## サイトの文章に「意思」を出す

7項目の人物像（年間100件のB2B受注を取るリーダー）に対して、サイトの語彙を測ったところ2つ穴があった。

| 測ったもの | 結果 |
|---|---|
| 合意 / 稟議 | 20 / 9 |
| 不安・反対・心配・負担・温度差・巻き込み・味方・根回し・社内政治・面子 | **すべて 0** |
| 「分からないまま」/「見えていない」/「仮説」 | 0 / 2 / 10 |
| 否定形「ません。」 | 243（ただし全部が世の中の観察。自分の宣言は2箇所のみ） |

つまり**制度は語れているが人の感情が一度も出てこない**、**全部見えている人として書かれている**の2点。足すべきは断定ではなく、不確実性と人だった。

最初に「測れない提案はしません」型の断り文で試したが却下。隙がなく防御的で、信頼につながらない。採用したのは「7割見えたら動きます。外れたら直します」型。

**適用9箇所**（会社概要のValues3本・強みのリードと01・トップのアプローチ2本・料金の目安・代表プロフィール）

- 「重視します／徹底し／期待に応える支援を行います」→「7割見えた時点で仮説を置いて、動かしながら直します」「最初の設計は、だいたいどこか外れます」「続けるより先に言います」
- 「私たちが選ばれる3つの理由を、ご紹介します」→「40社やってきて、効いたのはこの3つでした」
- 強み01に「現場は少し身構えます。『また入力が増えるのか』と」を追加
- 料金の目安に「迷ったら、いま一番困っている場面から選んで構いません。順番は後から変えられます」

**触らなかった場所**: Revenue CRMのLP・サービスページのリード・記事の「最初の一手」は、すでにこのregisterで書けていた。FAQと契約フローの敬語は事務手続きとして正しいので残す。

## 新規記事5本（CRM/RevOps以外の領域）

記事24本が全部CRM・RevOpsで、横断性が出ていなかった。note にすでに5領域とも存在していたので、そこを根にした。

| slug | カテゴリ | note の根 |
|---|---|---|
| `why-removing-anxiety-sells-in-japan-b2b` | 業界構造 | 日本市場で売れるウェブサイトは「不安を消す装置」だった／どこで戦うか |
| `why-marketing-budget-is-treated-as-cost` | 財務 | お金を受け取ることへの罪悪感は、あなたのせいじゃない |
| `why-talent-development-depends-on-individuals` | 組織 | 教えるのがしんどかった私が／LinkedIn投稿の品質基準を標準化 |
| `what-changes-in-crm-design-when-ai-writes-records` | AI | Claude Code の実運用／AIは仕事の進め方を変えるもの |
| `why-crm-investment-does-not-pass-the-board` | 経営 | My Mission -自分で選ぶ人を増やす- |

各1,000行前後・本文3,800〜5,000字。`_template` 準拠（定義ブロック・FAQ・Articleスキーマすべて出力を確認）。`insights/index.astro` に登録し、`categoryOrder` に経営・財務・組織・AI・業界構造を追加。

## アイキャッチ生成スクリプトの不具合2件（修正済み）

**1. テンプレート由来の記事は必ず生成に失敗していた**
`generate-eyecatches.mjs` は `headline: '文字列'` しか読めないのに、`_template` は `headline: title` の変数参照。`skip (no headline)` で無言スキップされる。参照先の `const` を辿る fallback を追加。

**2. スクリプトを走らせるとロゴ配色が巻き戻る（重大）**
`3e78dd5「コラムのアイキャッチ44枚をロゴの配色に合わせる」`で画像だけ赤 `#E62E45`・紫 `#765FC4` に直され、**スクリプトが取り残されていた**。全再生成すると44枚が紺 `#1E3A8A`・藍 `#4F46E5` に戻る。PALETTE に `accent`（ロゴの赤）と `brandViolet` を追加して同期。現在は全再生成してもバイト一致で差分ゼロ。

**3. ダッシュ置換の未発見の余波**
`what-is-revenue-visibility` の headline が `——` → `。` になり、アイキャッチの主題・副題の分割が壊れていた。他8本と同じ `｜` 区切りに修正。

## 検証

- ビルド165ページ（160 → +5）全パス
- 新記事の内部リンク・画像参照130本を全数検査、切れなし
- サービス名9箇所を既存記事の表記に統一（`CRM / RevOps再設計支援`・`HubSpot導入・運用支援`・`運用定着・改善支援`・`B2Bマーケティング戦略 / GTM設計`）
- 新記事の4読点以上の文はゼロ、emダッシュゼロ
- 既存アイキャッチ44枚に差分なし
